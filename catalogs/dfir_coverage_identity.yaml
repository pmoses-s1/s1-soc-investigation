name: 'DFIR: Coverage & identity (library)'
queries:
- id: cov-02-2
  title: Okta Source Coverage
  notes: 'What Okta data exists for this user? First/last event, count, countries, IPs. | Source: okta | Pivot if positive: Okta tab for full timeline'
  pq: 'serverHost=''{{src_okta|okta}}'' actor.alternateId=''{{entity}}''

    | let ts=(timestamp || published || event.time || sca:ingestTime)

    | filter ts = *

    | group FirstSeen.timestamp=min(ts), LastSeen.timestamp=max(ts), EventCount=count(), Countries=array_agg_distinct(client.geographicalContext.country, 10), IPs=array_agg_distinct(client.ipAddress, 10) by Source=serverHost

    | sort -EventCount

    | limit 100'
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: cov-03-2
  title: ZPA / Zscaler Session Coverage
  notes: 'What ZPA tunnel data exists? Confirms Zscaler was active and shows countries, public IPs. | Source: zscaler | Pivot if positive: ZIA tab for web history'
  pq: 'serverHost=''{{src_zscaler|zscaler}}''

    | filter actor.email contains:anycase(''{{entity}}'') OR actor.email contains:anycase(''{{username}}'') OR Username contains:anycase(''{{entity}}'') OR Username contains:anycase(''{{username}}'') OR event.users contains:anycase(''{{entity}}'') OR event.users contains:anycase(''{{username}}'')

    | let ts=(TimestampConnectionStart || timestamp || sca:ingestTime)

    | filter ts = *

    | group FirstSeen.timestamp=min(ts), LastSeen.timestamp=max(ts), EventCount=count(), Countries=array_agg_distinct(ClientCountryCode, 10), PublicIPs=array_agg_distinct(ClientPublicIP, 10) by Source=serverHost

    | sort -EventCount

    | limit 100'
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: cov-04-2
  title: ZIA Web Proxy Coverage
  notes: 'What ZIA data exists for this user? Confirms proxy was active. | Source: zia | Pivot if positive: ZIA tab'
  pq: 'serverHost=''{{src_zia|zia}}'' event.user=''{{entity}}''

    | let ts=(timestamp || event.time || sca:ingestTime)

    | filter ts = *

    | group FirstSeen.timestamp=min(ts), LastSeen.timestamp=max(ts), EventCount=count(), ClientIPs=array_agg_distinct(event.ClientIP, 10), Apps=array_agg_distinct(event.appname, 10) by Source=serverHost

    | sort -EventCount

    | limit 100'
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: cov-05-2
  title: EDR Source Coverage — by Username
  notes: 'What endpoint telemetry exists for this username? Confirms S1 agent was active. | Source: SentinelOne EDR | Pivot if positive: Endpoint tab'
  pq: '| filter src.process.user contains:anycase(''{{username}}'') OR event.login.userName contains:anycase(''{{username}}'')

    | let ts=(timestamp || event.time || published || sca:ingestTime)

    | filter ts = *

    | group FirstSeen.timestamp=min(ts), LastSeen.timestamp=max(ts), EventCount=count(), Endpoints=array_agg_distinct(endpoint.name, 20), AgentUUIDs=array_agg_distinct(agent.uuid, 20), EventTypes=array_agg_distinct(event.type, 20) by Source=serverHost

    | sort -EventCount

    | limit 100'
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: cov-06-2
  title: EDR Coverage Calendar — by Agent UUID
  notes: 'Was the endpoint active every day? Gaps in coverage could indicate device was off or agent disabled. | Source: SentinelOne EDR | Pivot if positive: Endpoint tab — check agent health dashboard for those dates'
  pq: '| filter agent.uuid == ''{{agent_uuid}}''

    | let ts=(timestamp || event.time || published || sca:ingestTime)

    | filter ts = *

    | group Events=count(), EventTypes=array_agg_distinct(event.type, 20), Users=array_agg_distinct(src.process.user, 10) by Day.timestamp=timebucket(ts, ''1d'')

    | sort -Day.timestamp

    | limit 1000'
  merge:
    kind: aggregate
    key_cols:
    - Day.timestamp
    - '''1d'')'
    sum_cols:
    - Events
  scope: subject
- id: cov-07-2
  title: Navan Travel Coverage
  notes: 'Does Navan have travel bookings for this user? Corroborates geo anomalies. | Source: navan-travel-bookings | Pivot if positive: Travel tab'
  pq: 'serverHost=''{{src_navan_travel_bookings|navan-travel-bookings}}''

    | filter passengers.person.email contains:anycase(''{{entity}}'') OR loggedAsUserName contains:anycase(''{{username}}'') OR booker.email contains:anycase(''{{entity}}'')

    | let ts=(timestamp || created || lastModified || sca:ingestTime)

    | filter ts = *

    | group FirstSeen.timestamp=min(ts), LastSeen.timestamp=max(ts), EventCount=count(), Purposes=array_agg_distinct(purpose, 10) by Source=serverHost

    | sort -EventCount

    | limit 100'
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: dlp-01-2
  title: DLP / Classification Event Coverage
  notes: 'Which DLP/classification-related fields appear for this user? | Source: DLP-capable sources | Pivot if positive: DLP-02 details, storage and endpoint timeline'
  pq: 'serverHost in (''google_workspace'',''google_workspace_prompt_drive'',''box'',''onedrive'',''prompt-security'',''zia'',''zscaler'')

    | filter actor.email contains:anycase(''{{entity}}'') OR login contains:anycase(''{{entity}}'') OR user contains:anycase(''{{entity}}'')

    | filter classificationLabel = * OR event.verdict.action = * OR event.alert.severity = * OR event.name contains:anycase(''dlp'') OR dlpdictionaries = *

    | group EventCount=count(), Labels=array_agg_distinct(classificationLabel, 20), Actions=array_agg_distinct(event.verdict.action || action, 20), Severities=array_agg_distinct(event.alert.severity || severity, 20) by Source=serverHost

    | sort -EventCount

    | limit 100'
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
  scope: subject
- id: foundation-unified-timeline
  title: Foundation - Unified User Timeline
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | columns ts.timestamp=(event.time || timestamp || id.time || published || event.datetime || time), Source=serverHost, User=(actor.email || event.user || src.process.user || user.name || detail.payload.Username || log.user || owner), Endpoint=(endpoint.name || event.devicehostname), Action=(event.name || event.type || action || EVENT_TYPE || detail.payload.Operation), Object=(event.doc_title || tgt.file.path || event.url || event.hostname || app_name || process.name), Detail=(src.process.cmdline || event.action || outcome.result || event.rulelabel) | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: foundation-user-source-coverage
  title: Foundation - User Source Coverage
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: placeholder-agent-uuid-pivot
  title: Placeholder Pivot - agent_uuid
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" agent.uuid="{{agent_uuid}}" | columns ts.timestamp=(event.time || timestamp || id.time || published || event.datetime), serverHost, endpoint.name, agent.uuid, user=(actor.email || event.user || src.process.user || user.name || detail.payload.Username), action=(event.name || event.type || action), object=(event.doc_title || tgt.file.path || event.url || event.hostname) | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: placeholder-devicehostname-pivot
  title: Placeholder Pivot - devicehostname
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.devicehostname="{{devicehostname}}" | columns ts.timestamp=(event.time || timestamp || id.time || published || event.datetime), serverHost, endpoint.name, agent.uuid, user=(actor.email || event.user || src.process.user || user.name || detail.payload.Username), action=(event.name || event.type || action), object=(event.doc_title || tgt.file.path || event.url || event.hostname) | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: placeholder-document-id-pivot
  title: Placeholder Pivot - document_id
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.doc_id="{{document_id}}" | columns ts.timestamp=(event.time || timestamp || id.time || published || event.datetime), serverHost, endpoint.name, agent.uuid, user=(actor.email || event.user || src.process.user || user.name || detail.payload.Username), action=(event.name || event.type || action), object=(event.doc_title || tgt.file.path || event.url || event.hostname) | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: placeholder-hostname-pivot
  title: Placeholder Pivot - hostname
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" endpoint.name="{{hostname}}" | columns ts.timestamp=(event.time || timestamp || id.time || published || event.datetime), serverHost, endpoint.name, agent.uuid, user=(actor.email || event.user || src.process.user || user.name || detail.payload.Username), action=(event.name || event.type || action), object=(event.doc_title || tgt.file.path || event.url || event.hostname) | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: placeholder-ip-pivot
  title: Placeholder Pivot - ip
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" ipAddress="{{ip}}" | columns ts.timestamp=(event.time || timestamp || id.time || published || event.datetime), serverHost, endpoint.name, agent.uuid, user=(actor.email || event.user || src.process.user || user.name || detail.payload.Username), action=(event.name || event.type || action), object=(event.doc_title || tgt.file.path || event.url || event.hostname) | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: personal-cloud-storage
  title: Exfiltration - Personal Cloud Storage
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('drive.google.com','dropbox.com','dropboxapi.com','icloud.com','mega.nz','mediafire.com','box.com','wetransfer.com','swisstransfer.com','gofile.io','file.io') | columns ts.timestamp=ts, event.user, event.deviceowner, event.devicehostname, host, event.url, event.appname, event.outbytes, event.action, event.rulelabel | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: usb-external-writes
  title: Exfiltration - USB / External Device Writes
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('File Creation','File Modification') | filter tgt.file.location == 'External Device' OR tgt.file.path contains:anycase('/Volumes/') | columns ts.timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), File=tgt.file.path, Ext=tgt.file.extension, Size=tgt.file.size, Proc=(src.process.name || process.name || ''), Cmd=(src.process.cmdline || process.cmdline || '') | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: ai-prompt-violations
  title: AI / LLM - Prompt Security Violations
  notes: 'Source: prompt-security | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_prompt_security|prompt-security}}' | filter log.isValid == true | let ts=(log.timestamp || log.event_time || event_time || timestamp || ReceiveTime) | filter log.violations = * OR log.violationFindingsDetails.Secrets = * OR log.violationFindingsDetails.Sensitive_Data = * OR log.violationFindingsDetails.Harmful_Content_Moderator = * | columns ts.timestamp=ts, log.user, log.userGroups, log.application.name, log.sensor.pageDomain, log.sensor.pageUrl, log.ipAddress, log.country, log.policyName, log.violations, log.violationFindingsDetails.Secrets, log.violationFindingsDetails.Sensitive_Data, log.conversationId, log.promptResponseId, severity | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: google-drive-download-external
  title: Google Workspace - Drive Download / External Sharing
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_google_workspace|google_workspace}}' | filter event.name in:anycase('download','sync_item_content','access_item_content','change_document_visibility','change_document_access_scope','shared_drive_file_downloaded','copy','print') OR event.visibility in:anycase('shared_externally','public') | columns ts.timestamp=(id.time || timestamp), actor.email, event.name, event.doc_title, event.doc_id, event.doc_type, event.owner, event.visibility, event.old_visibility, classificationLabel, ipAddress, actor.applicationInfo.applicationName | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: salesforce-report-export
  title: Salesforce - Report Export / File Activity
  notes: 'Source: salesforce-realtime | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_realtime|salesforce-realtime}}' | filter detail.payload.Username = * | filter detail.payload.ReportId = * OR detail.payload.RowsReturned > 0 OR detail.payload.RowsProcessed > 0 OR detail.payload.FileAction = * OR detail.payload.Operation contains:anycase('Export','Download') | columns ts.timestamp=(detail.payload.EventDate || detail.payload.CreatedDate || time || timestamp), User=detail.payload.Username, UserId=detail.payload.UserId, SourceIp=detail.payload.SourceIp, Operation=detail.payload.Operation, App=detail.payload.AppName, ReportId=detail.payload.ReportId, RowsReturned=detail.payload.RowsReturned, RowsProcessed=detail.payload.RowsProcessed, ContentSize=detail.payload.ContentSize, FileAction=detail.payload.FileAction, Query=detail.payload.Query, SessionKey=detail.payload.SessionKey | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: okta-mfa-factor-changes
  title: Okta - MFA / Factor Changes
  notes: 'Source: okta | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_okta|okta}}' | filter eventType contains:anycase('user.mfa','factor','system.mfa') OR displayMessage contains:anycase('factor','MFA','enroll','reset','deactivate') | columns ts.timestamp=(published || timestamp), actor.alternateId, eventType, displayMessage, client.ipAddress, client.geographicalContext.country, outcome.result, target.displayName | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: remote-access-execution
  title: Unauthorized Access - Remote Access Tool Execution
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('Process Creation','Process','Process Start') | let ts=(event.time || event_time || timestamp || ReceiveTime) | let User=(user.name || src.process.user || event.login.userName || '') | let Proc=(process.name || src.process.name || tgt.process.name || '') | let Cmd=(process.cmdline || src.process.cmdline || tgt.process.cmdline || '') | filter Proc contains:anycase('teamviewer','anydesk','rustdesk','screenconnect','connectwise','logmein','bomgar','beyondtrust','splashtop','vnc','tailscale','openvpn','wireguard','nordvpn','expressvpn','protonvpn','surfshark') OR Cmd contains:anycase('teamviewer','anydesk','rustdesk','tailscale','openvpn','wireguard','nordvpn') | columns ts.timestamp=ts, User, endpoint.name, agent.uuid, Proc, Cmd | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: ai-prompt-security-ai-apps-installed-zia-usage-candidate-list
  title: AI Apps Installed + ZIA Usage Candidate List
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union ( serverHost='{{src_oplockdown|oplockdown}}' | filter tag in ('AI-IDE','AI-MISUSE') | columns ts=timestamp, User=owner, Endpoint=endpoint.name, Source='OpLockdown', App=app_name, Detail=tag, Bytes=0 ), ( serverHost='{{src_zia|zia}}' | filter event.appname contains:anycase('Claude','Anthropic','Codeium','Windsurf','Glean','OpenAI','ChatGPT','Gemini','Copilot','Perplexity','Cursor') OR event.hostname contains:anycase('claude.ai','anthropic.com','openai.com','chatgpt.com','gemini.google.com','perplexity.ai','cursor.com','cursor.sh','windsurf.ai','codeium.com') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | columns ts=(event.datetime || timestamp), User=event.user, Endpoint=event.devicehostname, Source='ZIA', App=event.appname, Detail=event.hostname, Bytes=outB ) | sort -ts | nolimit
  merge:
    kind: append
  scope: subject
- id: ai-prompt-security-prompt-violations-drilldown
  title: Prompt Violations Drilldown
  notes: 'Source: prompt-security | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost == 'prompt-security' | filter log.isValid == true | let ts=(log.timestamp || log.event_time || event_time || timestamp || ReceiveTime) | filter log.violations = * OR log.violationFindingsDetails.Secrets = * OR log.violationFindingsDetails.Sensitive_Data = * OR log.violationFindingsDetails.Harmful_Content_Moderator = * | columns ts, log.user, log.userGroups, log.application.name, log.sensor.pageDomain, log.sensor.pageUrl, log.ipAddress, log.country, log.policyName, log.violations, log.violationFindingsDetails.Secrets, log.violationFindingsDetails.Sensitive_Data, log.violationFindingsDetails.Harmful_Content_Moderator, log.conversationId, log.promptResponseId, severity | sort -ts | nolimit
  merge:
    kind: append
  scope: subject
- id: correlation-timelines-installed-high-risk-software-same-user-zia-egres
  title: Installed High-Risk Software + Same User ZIA Egress Timeline
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union ( serverHost='{{src_oplockdown|oplockdown}}' | filter tag in ('VPN','RemoteManagement','Filesharing','Peer-To-Peer','AI-IDE','AI-MISUSE') | columns ts.timestamp=timestamp, User=owner, endpoint.name, signal='oplockdown_installed', object=app_name, detail=tag, bytes=0 ), ( serverHost='{{src_zia|zia}}' | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter outB > 5000000 OR event.hostname contains:anycase('icloud','dropbox','wetransfer','mega','github','gitlab','anthropic','openai','chatgpt','perplexity') | columns ts.timestamp=(event.datetime || timestamp), User=event.user, endpoint.name=event.devicehostname, signal='zia_egress', object=(event.hostname || event.url), detail=(event.appname || event.action), bytes=outB ) | sort User, -ts.timestamp | nolimit0
  merge:
    kind: append
  scope: subject
- id: correlation-timelines-sensitive-access-endpoint-staging-zia-egress
  title: Sensitive Access + Endpoint Staging + ZIA Egress
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union ( serverHost='{{src_google_workspace|google_workspace}}' | filter classificationLabel in:anycase('Highly Confidential','Confidential') OR event.name in:anycase('download','change_document_visibility','change_document_access_scope') | let user_name=(actor.email || event.owner || '') | columns ts.timestamp=(timestamp || id.time || event.time), user_name, signal='workspace_sensitive_activity', object=(event.doc_title || event.doc_id || '-'), detail=(event.name || classificationLabel), endpoint='-', bytes=0 ), ( event.type in ('File Creation','File Modification') | filter tgt.file.extension in:anycase('zip','rar','7z','tar','gz','tgz','pst','mbox') OR tgt.file.path contains:anycase('\\Downloads\\') OR tgt.file.path contains:anycase('\\Desktop\\') OR tgt.file.path contains:anycase('\\Documents\\') OR tgt.file.path contains:anycase('/Downloads/') OR tgt.file.path contains:anycase('/Desktop/') OR tgt.file.path contains:anycase('/Documents/') OR tgt.file.path contains:anycase('/tmp/') | let user_name=(src.process.user || user.name || '') | columns ts.timestamp=(event.time || timestamp), user_name, signal='endpoint_staging', object=(tgt.file.path || '-'), detail=(tgt.file.extension || src.process.name), endpoint=endpoint.name, bytes=(tgt.file.size || 0) ), ( serverHost='{{src_zia|zia}}' | let user_name=(event.user || '') | let url=(event.url || event.hostname || event.dns_req || '-') | let method=(event.requestmethod || event.method || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000 | columns ts.timestamp=(event.datetime || timestamp || sca:ingestTime), user_name, signal='zia_egress', object=url, detail=(event.action || event.appname), endpoint=event.devicehostname, bytes=outB ) | sort user_name, -ts.timestamp | nolimit0
  merge:
    kind: append
  scope: subject
- id: correlation-sensitive-journey-sensitive-access-endpoint-staging-zia-eg
  title: Sensitive Access + Endpoint Staging + ZIA Egress
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union ( serverHost == 'google_workspace' | filter classificationLabel in:anycase('Highly Confidential','Confidential') | filter event.name in:anycase('download','sync_item_content','access_item_content','view','copy','print','change_document_visibility','change_document_access_scope') | let user_name=(actor.email || event.owner || '') | columns timestamp=(timestamp || id.time || event.time), user_name, signal='workspace_sensitive_activity', object=(event.doc_title || event.doc_id || '-'), detail=(event.name || classificationLabel), endpoint='-', bytes=0 ), ( serverHost='{{src_salesforce_realtime|salesforce-realtime}}' | filter detail.payload.RowsReturned > 0 OR detail.payload.RowsProcessed > 0 OR detail.payload.ReportId = * | let user_name=detail.payload.Username | columns timestamp=(detail.payload.EventDate || detail.payload.CreatedDate || timestamp), user_name, signal='salesforce_report_or_rows', object=(detail.payload.ReportId || detail.payload.Query || '-'), detail=(detail.payload.Operation || detail.payload.AppName), endpoint='-', bytes=(detail.payload.ContentSize || 0) ), ( | filter event.type == 'File Creation' AND (tgt.file.extension in:anycase('zip','rar','7z','tar','gz','tgz','pst','mbox') OR tgt.file.path matches '(?i).*/(Downloads|Desktop|Documents|tmp|Temp)/.*' OR tgt.file.path matches '(?i).*\\Users\\.*\\(Downloads|Desktop|Documents)\\.*') | let user_name=(src.process.user || user.name || '') | columns timestamp=(event.time || timestamp), user_name, signal='endpoint_staging', object=(tgt.file.path || '-'), detail=(tgt.file.extension || src.process.name), endpoint=endpoint.name, bytes=(tgt.file.size || 0) ), ( serverHost == 'zia' | let user_name=(event.user || '') | let url=(event.url || event.hostname || event.dns_req || '-') | let method=(event.requestmethod || event.method || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | filter isUpload | columns timestamp=(event.datetime || timestamp || sca:ingestTime), user_name, signal='zia_egress', object=url, detail=(event.action || event.appname), endpoint=event.devicehostname, bytes=outB ) | sort user_name, timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: correlation-software-egress-installed-high-risk-software-same-user-zia
  title: Installed High-Risk Software + Same User ZIA Egress Timeline
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union ( serverHost='{{src_oplockdown|oplockdown}}' | filter tag in ('VPN','RemoteManagement','Filesharing','Peer-To-Peer','AI-IDE','AI-MISUSE','Games') | columns ts=timestamp, User=owner, Endpoint=endpoint.name, Source='OpLockdown', Signal=tag, Object=app_name, Detail=type, Bytes=0 ), ( serverHost='{{src_zia|zia}}' | let host=(event.hostname || event.url || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter outB > 5000000 OR host contains:anycase('icloud','dropbox','wetransfer','swisstransfer','mega','mediafire','claude','chatgpt','openai','perplexity','cursor','tailscale','teamviewer','anydesk','rustdesk') | columns ts=(event.datetime || timestamp || sca:ingestTime), User=event.user, Endpoint=event.devicehostname, Source='ZIA', Signal=event.appname, Object=host, Detail=event.action, Bytes=outB ) | sort User, ts | nolimit
  merge:
    kind: append
  scope: subject
- id: correlation-software-egress-potential-block-candidates-by-app-destinat
  title: Potential Block Candidates by App / Destination
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union ( serverHost='{{src_oplockdown|oplockdown}}' | filter tag in ('VPN','RemoteManagement','Filesharing','Peer-To-Peer','AI-IDE','AI-MISUSE','Games') | group InstalledEndpoints=estimate_distinct(endpoint.name), InstalledUsers=estimate_distinct(owner), LastInstalled.timestamp=max(lastseen) by App=app_name, Category=tag ), ( serverHost='{{src_zia|zia}}' | let host=(event.hostname || event.url || event.dns_req || '') | let app=(event.appname || host) | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter host contains:anycase('icloud','wetransfer','swisstransfer','mega','dropbox','mediafire','telegram','discord','signal','claude','chatgpt','openai','perplexity','cursor','tailscale','teamviewer','anydesk','rustdesk','utorrent') OR app contains:anycase('iCloud','Dropbox','Claude','ChatGPT','Perplexity','Cursor','Tailscale','TeamViewer','AnyDesk','RustDesk','uTorrent') | group ZIAEvents=count(), ZIAUsers=estimate_distinct(event.user), UploadBytes=sum(outB), Hosts=array_agg_distinct(host,20) by App=app, Category='ZIA Destination' ) | group InstalledEndpoints=sum(InstalledEndpoints), InstalledUsers=sum(InstalledUsers), ZIAEvents=sum(ZIAEvents), ZIAUsers=sum(ZIAUsers), UploadBytes=sum(UploadBytes), Hosts=array_agg_distinct(Hosts,20), LastInstalled.timestamp=max(LastInstalled.timestamp) by App, Category | sort -UploadBytes, -InstalledEndpoints | nolimit
  merge:
    kind: aggregate
    key_cols:
    - App
    - Category
    - ( serverHost
    - '''wetransfer'''
    - '''swisstransfer'''
    - '''mega'''
    - '''dropbox'''
    - '''mediafire'''
    - '''telegram'''
    - '''discord'''
    max_cols:
    - LastInstalled.timestamp
    approx_cols:
    - InstalledEndpoints
    - InstalledUsers
  scope: subject
- id: edr-staging-tooling-large-archive-creation-50mb
  title: Large Archive Creation >50MB
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter event.type == 'File Creation' AND tgt.file.extension in:anycase('zip','rar','7z','tar','gz','tgz','pst','mbox') | let Size=(tgt.file.size || file.size || 0) | filter Size > 50000000 | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), File=tgt.file.path, Ext=tgt.file.extension, SizeMB=(Size/1024/1024), Proc=(src.process.name || src.process.image.path), Cmd=src.process.cmdline | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: edr-staging-tooling-remote-access-vpn-p2p-games-execution
  title: Remote Access / VPN / P2P / Games Execution
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter event.type in ('Process Creation','Process Start','Process') OR dataSource.category == 'process' | let Proc=(src.process.name || tgt.process.name || process.name || '') | let Cmd=(src.process.cmdline || tgt.process.cmdline || process.cmdline || '') | filter Proc contains:anycase('teamviewer','anydesk','vnc','screenconnect','connectwise','logmein','bomgar','beyondtrust','splashtop','rustdesk','ngrok','tailscale','wireguard','openvpn','nordvpn','protonvpn','utorrent','qbittorrent','transmission','steam','epic','battle.net') OR Cmd contains:anycase('teamviewer','anydesk','rustdesk','tailscale','wireguard','openvpn','ngrok','utorrent','qbittorrent') | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc, Cmd, Parent=(src.process.parent.name || '') | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: edr-staging-tooling-transfer-exfil-tool-execution
  title: Transfer / Exfil Tool Execution
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter event.type in ('Process Creation','Process Start','Process') OR dataSource.category == 'process' | let Proc=(src.process.name || tgt.process.name || process.name || '') | let Cmd=(src.process.cmdline || tgt.process.cmdline || process.cmdline || '') | filter Proc contains:anycase('rclone','scp','sftp','rsync','curl','wget','aws','gsutil','megacmd','megasync','onedrive','dropbox','drivefs','box','icloud') OR Cmd contains:anycase(' rclone ',' scp ',' sftp ',' rsync ',' curl ',' wget ',' aws s3 ',' gsutil ',' mega-put ',' mega-cp ',' onedrive ',' dropbox ',' icloud ') | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc, Cmd, Parent=(src.process.parent.name || ''), Path=(src.process.image.path || tgt.process.image.path || '') | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: edr-staging-tooling-usb-external-device-writes
  title: USB / External Device Writes
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter event.type in ('File Creation','File Modification') | filter tgt.file.location == 'External Device' OR tgt.file.path contains:anycase('/Volumes/') | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), File=tgt.file.path, Ext=tgt.file.extension, Size=tgt.file.size, Proc=(src.process.name || process.name || ''), Cmd=(src.process.cmdline || process.cmdline || '') | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: executive-summary-combined-high-signal-timeline
  title: Combined High-Signal Timeline
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union ( serverHost='{{src_oplockdown|oplockdown}}' | filter tag in ('VPN','RemoteManagement','Filesharing','Peer-To-Peer','AI-IDE','AI-MISUSE','Games') | columns ts=timestamp, Source='OpLockdown', User=owner, Endpoint=endpoint.name, Signal=tag, Object=app_name, Detail=type, Bytes=0 ), ( serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let host=(event.hostname || event.url || event.dns_req || '') | filter outB > 5000000 OR host contains:anycase('icloud','dropbox','wetransfer','swisstransfer','mega','mediafire','pastebin','gist.github','claude','chatgpt','openai','perplexity','cursor','anthropic') | columns ts, Source='ZIA', User=event.user, Endpoint=event.devicehostname, Signal=(event.appname || event.urlcategory), Object=host, Detail=(event.action || event.requestmethod), Bytes=outB ), ( serverHost='{{src_prompt_security|prompt-security}}' | filter log.isValid == true | filter log.violations = * OR log.violationFindingsDetails.Secrets = * OR log.violationFindingsDetails.Sensitive_Data = * | let ts=(log.timestamp || log.event_time || event_time || timestamp || ReceiveTime) | columns ts, Source='Prompt Security', User=log.user, Endpoint=log.sensor.machine, Signal=(log.policyName || 'Prompt violation'), Object=(log.application.name || log.sensor.pageDomain), Detail=log.violations, Bytes=0 ), ( event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | columns ts=(event.time || timestamp), Source='EDR iCloud', User=src.process.user, Endpoint=endpoint.name, Signal=event.type, Object=tgt.file.path, Detail=src.process.name, Bytes=(tgt.file.size || 0) ) | sort -ts | nolimit
  merge:
    kind: append
  scope: subject
- id: executive-summary-high-risk-software-installed
  title: High-Risk Software Installed
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_oplockdown|oplockdown}}' | filter tag in ('VPN','RemoteManagement','Filesharing','Peer-To-Peer','AI-IDE','AI-MISUSE','Games') | group count()
  merge:
    kind: append
  scope: subject
- id: executive-summary-large-zia-uploads-50mb
  title: Large ZIA Uploads >50MB
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter outB > 50000000 | group count()
  merge:
    kind: append
  scope: subject
- id: executive-summary-prompt-security-violations
  title: Prompt Security Violations
  notes: 'Source: prompt-security | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_prompt_security|prompt-security}}' | filter log.isValid == true | filter log.violations = * OR log.violationFindingsDetails.Secrets = * OR log.violationFindingsDetails.Sensitive_Data = * OR log.violationFindingsDetails.Harmful_Content_Moderator = * | group count()
  merge:
    kind: append
  scope: subject
- id: executive-summary-salesforce-export-report-events
  title: Salesforce Export / Report Events
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost in ('salesforce-realtime','salesforce_event') | filter EVENT_TYPE contains:anycase('ReportExport') OR detail.payload.Operation contains:anycase('Export') OR detail.payload.FileAction contains:anycase('Download') OR detail.payload.RowsReturned > 0 OR detail.payload.RowsProcessed > 0 | group count()
  merge:
    kind: append
  scope: subject
- id: executive-summary-icloud-file-events
  title: iCloud File Events
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | group count()
  merge:
    kind: append
  scope: subject
- id: ai-unsanctioned-app-usage
  title: AI / Unsanctioned App Usage
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.urlcategory contains:anycase('AI','ML') OR event.appname contains:anycase('AI','Claude','ChatGPT','Gemini','Perplexity','Copilot') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | group upload_bytes=sum(outB), events=count() by event.user,event.hostname,event.appname,event.action | sort -upload_bytes
  merge:
    kind: aggregate
    key_cols:
    - event.user
    - event.hostname
    - event.appname
    - event.action
    sum_cols:
    - upload_bytes
    - events
  scope: subject
- id: corr-04
  title: AI App Installed + AI Network + Prompt Security
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: 'any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, ''Source\User'') contains "{{entity}}" Step 1: OPL-03 tag in (''AI-IDE'',''AI-MISUSE''). Step 2: ZIA-AI-01 / ZIA-AI-02. Step 3: Prompt Security user/domain violation panels.'
  merge:
    kind: append
  scope: subject
- id: ai-genai-hunting-ai-applications-installed
  title: AI Applications Installed
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_oplockdown|oplockdown}}' | filter tag in ('AI-IDE','AI-MISUSE') OR app_name contains:anycase('Cursor','Perplexity','Claude','ChatGPT','Windsurf','Copilot','Gemini','Antigravity') | group Endpoints=estimate_distinct(endpoint.name), Users=estimate_distinct(owner), LastSeen.timestamp=max(lastseen) by app_name, tag | sort -Endpoints | nolimit
  merge:
    kind: aggregate
    key_cols:
    - app_name
    - tag
    max_cols:
    - LastSeen.timestamp
    approx_cols:
    - Endpoints
    - Users
  scope: subject
- id: ai-genai-hunting-ai-network-usage-by-user
  title: AI Network Usage by User
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.appname contains:anycase('Claude','Anthropic','Codeium','Windsurf','Glean','OpenAI','ChatGPT','Gemini','Copilot','Perplexity','Cursor') OR event.hostname contains:anycase('claude.ai','anthropic.com','openai.com','chatgpt.com','gemini.google.com','perplexity.ai','cursor.com','cursor.sh','windsurf') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | group upload_bytes=sum(outB), events=count(), destinations=estimate_distinct(event.hostname) by event.user, event.devicehostname, event.appname | sort -upload_bytes | nolimit
  merge:
    kind: aggregate
    key_cols:
    - event.user
    - event.devicehostname
    - event.appname
    sum_cols:
    - upload_bytes
    - events
    approx_cols:
    - destinations
  scope: subject
- id: aws-01-2
  title: AWS CloudTrail Identity Coverage
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: serverHost in ('DataSet-CloudTrail','OW-CloudTrail','nglz-cloudtrail','observo-cloudtrail','infosec-sandbox-org-cloudtrail','infosec-attivo-cloudtrail','pingsafe-cloudtrail','promptsecurity-cloudtrail') | filter userIdentity.arn contains:anycase('{{entity}}') OR userIdentity.sessionContext.sessionIssuer contains:anycase('{{entity}}') OR userIdentity.userName contains:anycase('{{entity}}') | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp), Accounts=array_agg_distinct(recipientAccountId, 20), EventNames=array_agg_distinct(eventName, 20), IPs=array_agg_distinct(sourceIPAddress, 20) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
    array_cols:
    - Accounts
    - EventNames
    - IPs
  scope: subject
- id: aws-02-2
  title: AWS High-Risk API Calls
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: serverHost contains 'cloudtrail' | filter userIdentity.arn contains:anycase('{{entity}}') OR userIdentity.sessionContext.sessionIssuer contains:anycase('{{entity}}') | filter eventName in ('CreateAccessKey','AttachUserPolicy','PutUserPolicy','CreatePolicyVersion','AddUserToGroup','AssumeRole','PassRole','StopLogging','DeleteTrail','PutEventSelectors','GetObject','ListBucket','PutBucketPolicy') | sort -timestamp | nolimit | columns Time=simpledateformat(timestamp,'yyyy-MM-dd HH:mm:ss'), Source=serverHost, Account=recipientAccountId, Event=eventName, User=userIdentity.arn, Issuer=userIdentity.sessionContext.sessionIssuer.userName, IP=sourceIPAddress, UA=userAgent, Error=errorCode, Request=requestParameters
  merge:
    kind: append
  scope: subject
- id: zia-dlp-16-2
  title: After Hours Upload Activity
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let hour=hourofday(ts) | let method=(event.requestmethod || event.method || '') | let url=(event.url || event.hostname || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | filter isUpload AND (hour < 7 OR hour > 19) | columns ts, event.user, event.deviceowner, event.devicehostname, event.hostname, event.url, event.appname, outB, event.action, event.rulelabel | sort -ts | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-up-03-2
  title: After-Hours Upload Activity
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let hr=hourofday(timestamp) | filter outB > 5000000 AND (hr < 6 OR hr > 20) | group upload_bytes=sum(outB), events=count() by event.user,event.hostname,event.appname | sort -upload_bytes
  merge:
    kind: aggregate
    key_cols:
    - event.user
    - event.hostname
    - event.appname
    sum_cols:
    - upload_bytes
    - events
  scope: subject
- id: cov-01-2
  title: All Log Sources — Count by serverHost
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" any( actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, ipAddress, ip, session, event.client_id, event.app_name, event.api_name, id.applicationName, "Source\ User" ) contains 'username' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: zia-dlp-26-2
  title: All ZIA Raw Activity
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | columns ts, event.user, event.deviceowner, event.devicehostname, event.requestmethod, event.method, event.url, event.hostname, event.appname, event.urlcategory, event.urlsupercategory, event.destcountry, event.action, event.rulelabel, event.inbytes, event.outbytes, event.threatcat, event.threatname, event.threat_score, event.threat_severity | sort -ts | nolimit0
  merge:
    kind: append
  scope: subject
- id: zia-dlp-22-2
  title: Allowed High-Risk Destinations
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let host=(event.hostname || event.url || event.dns_req || '') | filter event.action in ('Allow','Allowed','ALLOW') | filter host contains:anycase('wetransfer','mega','mediafire','pastebin','gist.github','dropbox','drive.google','icloud','github','gitlab','bitbucket') | group events=count(), users=estimate_distinct(event.user), total_out=sum(event.outbytes || 0) by host, event.appname, event.urlcategory, event.rulelabel | sort -total_out | nolimit
  merge:
    kind: aggregate
    key_cols:
    - host
    - event.appname
    - event.urlcategory
    - event.rulelabel
    sum_cols:
    - events
    - total_out
    approx_cols:
    - users
  scope: subject
- id: policy-review-rollups-allowed-high-risk-destinations
  title: Allowed High-Risk Destinations
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let host=(event.hostname || event.url || event.dns_req || '') | filter event.action in ('Allow','Allowed','ALLOW') | filter host contains:anycase('wetransfer','mega','mediafire','pastebin','gist.github','dropbox','drive.google','icloud','github','gitlab','bitbucket','telegram','signal') | group events=count(), users=estimate_distinct(event.user), total_out=sum(event.outbytes || 0) by host, event.appname, event.urlcategory, event.rulelabel | sort -total_out | nolimit
  merge:
    kind: aggregate
    key_cols:
    - host
    - event.appname
    - event.urlcategory
    - event.rulelabel
    sum_cols:
    - events
    - total_out
    approx_cols:
    - users
  scope: subject
- id: corr-02
  title: Archive Created then ZIA Upload
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: 'any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, ''Source\User'') contains "{{entity}}" Run ZIA-DLP Endpoint tab: Large Archive Creation (>50MB), Archive Outputs Created, then Potential Upload Activity / Large Uploads for the same user/device/time window.'
  merge:
    kind: append
  scope: subject
- id: zia-dlp-39-2
  title: Archive Deletes / Renames
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type contains:anycase('Delete','Rename','Move') ) | filter( tgt.file.extension in:anycase('7z','zip','rar','pst','mbox','tgz','gz','tar','tar.gz') OR tgt.file.path matches '(?i).*\\\\Users\\\\.*\\\\(Downloads|Desktop|Documents)\\\\.*' OR tgt.file.path matches '(?i).*/(Downloads|Desktop|Documents|tmp|Temp)/.*' ) | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Action=event.type, File=tgt.file.path, Proc=(src.process.name || src.process.image.path), Cmd=src.process.cmdline | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-30-2
  title: Archive Outputs Created
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'File Creation' AND tgt.file.extension in:anycase('7z','zip','rar','pst','mbox','tgz','gz','tar','tar.gz') ) | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), File=tgt.file.path, Ext=tgt.file.extension, Size=tgt.file.size, Proc=(src.process.name || src.process.image.path), Cmd=src.process.cmdline | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: atl-01-2
  title: Atlassian/Jira/Confluence Coverage
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: serverHost in ('atlassian-cloud-enterprise','jira','confluence-cloud') | filter actor.email contains:anycase('{{entity}}') OR actor.name contains:anycase('{{entity}}') OR user contains:anycase('{{entity}}') | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp), Actions=array_agg_distinct(action, 20) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
    array_cols:
    - Actions
  scope: subject
- id: zia-dlp-03-2
  title: Blocked / Reset Upload Attempts
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let method=(event.requestmethod || event.method || '') | let url=(event.url || event.hostname || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | filter isUpload AND !(event.action in ('Allow','Allowed','ALLOW')) | group count()
  merge:
    kind: append
  scope: subject
- id: zia-dlp-21-2
  title: Blocked vs Allowed by Destination
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | group events=count(), users=estimate_distinct(event.user), total_out=sum(event.outbytes || 0) by event.action, event.hostname, event.appname, event.rulelabel | sort -events | nolimit
  merge:
    kind: aggregate
    key_cols:
    - event.action
    - event.hostname
    - event.appname
    - event.rulelabel
    sum_cols:
    - events
    - total_out
    approx_cols:
    - users
  scope: subject
- id: imported-searches-box-search-66
  title: Box search 66
  notes: 'Source: box | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union ( serverHost='{{src_box|box}}' | let ts=(created_at || timestamp) | columns timestamp=ts, Source=serverHost, User=(action_by.login || created_by.login), Action=event_type, File=source.item_name, IP=(ip_address || source_ip), Details=additional_details ), ( serverHost='{{src_onedrive|onedrive}}' | let ts=(timestamp || event.time || createdDateTime) | columns timestamp=ts, Source=serverHost, User=(auditData.UserId || userPrincipalName), Action=(operation || auditData.Operation), File=(auditData.SourceFileName || auditData.ObjectId), IP=(auditData.ClientIP || clientIP), Details=auditData ) | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: saa-01-2
  title: Broad SaaS Coverage
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: serverHost in ('zoom','zoom_signin','zoom_meeting_details_jh','zoom_meeting_user','figma','netsuite','pagerduty','asana_audit_log_jh','monday_audit_log_jh','clari_audit_log_jh','contentstack','goanywheremft') | filter any( actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, email, login, elogin, account, operator, src.process.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, ipAddress, ip, session, event.client_id, event.app_name, event.api_name, id.applicationName, "Source\ User" ) contains '{{entity}}' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp), Events=array_agg_distinct(event.name || action || event_type, 20) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
    array_cols:
    - Events
  scope: subject
- id: zia-dlp-29-2
  title: Bulk File Creation Spike (15m)
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'File Creation' ) | group Files=count(), TotalBytes=sum(tgt.file.size) by bucket=timebucket(event.time,'15m'), endpoint.name, User=(src.process.user || user.name) | filter( Files >= 50 OR TotalBytes >= 100000000 ) | sort -bucket | nolimit
  merge:
    kind: aggregate
    key_cols:
    - bucket
    - '''15m'')'
    - endpoint.name
    - User
    sum_cols:
    - Files
    - TotalBytes
  scope: subject
- id: zia-dlp-41-2
  title: Cleanup / Wiping Tools
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'Process Creation' ) | let Proc=(src.process.name || tgt.process.name || '') | filter( Proc contains:anycase('bleachbit','ccleaner','sdelete','eraser','wipe','shred','privazer','cleanmymac') ) | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc, Path=(src.process.image.path || tgt.process.image.path), Cmd=(src.process.cmdline || tgt.process.cmdline) | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: anti-forensics-cleanup-wiping-tools
  title: Cleanup / Wiping Tools
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('Process Creation','Process') | let Proc=(src.process.name || tgt.process.name || '') | filter Proc contains:anycase('bleachbit','ccleaner','sdelete','eraser','wipe','shred','privazer','cleanmymac') | columns ts.timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc, Path=(src.process.image.path || tgt.process.image.path), Cmd=(src.process.cmdline || tgt.process.cmdline) | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: endpoint-exfil-tooling-cloud-cli-sync-tooling
  title: Cloud CLI / Sync Tooling
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('Process Creation','Process Start','Process') | let Proc=(src.process.name || tgt.process.name || process.name || '') | let Cmd=(src.process.cmdline || tgt.process.cmdline || process.cmdline || '') | filter Proc contains:anycase('aws','gsutil','megacmd','megasync','boxdrive','drivefs','googledrive','onedrive','dropbox','icloud') OR Cmd contains:anycase(' aws s3 ',' gsutil ',' mega-put ',' mega-cp ',' drivefs ',' googledrive ',' onedrive ',' dropbox ',' icloud ') | columns ts.timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc, Cmd, Parent=(src.process.parent.name || ''), Path=(src.process.image.path || tgt.process.image.path || '') | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-33-2
  title: Cloud CLI Tooling (aws / gsutil / megacmd / mega / box / drivefs)
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'Process Creation' OR event.type == 'Process Start' OR dataSource.category == 'process' ) | let Proc=(src.process.name || tgt.process.name || process.name || '') | let Cmd=(src.process.cmdline || tgt.process.cmdline || process.cmdline || '') | filter( Proc contains:anycase('aws','aws.exe','gsutil','megacmd','megasync','box','boxdrive','drivefs','googledrive','onedrive','dropbox','icloud') OR Cmd contains:anycase(' aws s3 ',' gsutil ',' mega-put ',' mega-cp ',' box ',' drivefs ',' googledrive ',' onedrive ',' dropbox ',' icloud ') ) | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc, Cmd, Parent=(src.process.parent.name || ''), Path=(src.process.image.path || tgt.process.image.path || '') | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: lkp-03-2
  title: Cost Center / HR Context
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: '| dataset ''config://datatables/CostCenters'' | filter email == ''{{entity}}'' | columns email, Hire_Date'
  merge:
    kind: append
  scope: subject
- id: dlp-01-2-2
  title: DLP / Classification Event Coverage
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: serverHost in ('google_workspace','google_workspace_prompt_drive','box','onedrive','prompt-security','zia','zscaler') | filter actor.email contains:anycase('{{entity}}') OR login contains:anycase('{{entity}}') OR user contains:anycase('{{entity}}') | filter classificationLabel = * OR event.verdict.action = * OR event.alert.severity = * OR event.name contains:anycase('dlp') OR dlpdictionaries = * | group EventCount=count(), Labels=array_agg_distinct(classificationLabel, 20), Actions=array_agg_distinct(event.verdict.action || action, 20), Severities=array_agg_distinct(event.alert.severity || severity, 20) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    array_cols:
    - Labels
    - Actions
    - Severities
  scope: subject
- id: net-02-2
  title: DNS Queries by Host/IP
  notes: 'Source: dns-query-logs | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_dns_query_logs|dns-query-logs}}' | filter srcaddr == '{{ip}}' OR event.src_ip == '{{ip}}' OR k8s_node == 'S1LT-G35BSW3' OR question contains:anycase('S1LT-G35BSW3') | group Count=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Question=question, SourceIP=srcaddr | sort -Count | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Question
    - SourceIP
    sum_cols:
    - Count
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: lkp-02-2
  title: Dashboard Search Builder — Intune + Jamf
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: '| sql outer join windows=(| dataset ''config://datatables/Accounts_intune'' | group comp=array_agg_distinct(displayName) by email | columns email, list="actor.email = ''" + email + "'' or device.name in (''" + replace(comp.to_string(), '','', "'',''") + "'')" ), mac=(| dataset ''config://datatables/Accounts_Jamf'' | group comp=array_agg_distinct(Computer_Name) by email | columns email, list="actor.email = ''" + email + "'' or device.name in (''" + replace(comp.to_string(), '','', "'',''") + "'')") on email | filter email == ''{{entity}}'''
  merge:
    kind: aggregate
    key_cols:
    - email | columns email
    - list
    - ''''
    - ''''
    - '"'''
    - '''") + "'')" )'
    - mac
    - list
    - ''''
    - ''''
    array_cols:
    - comp
  scope: subject
- id: imported-searches-data-movement-search-63
  title: Data Movement search 63
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost in:anycase('zia','zscaler') | let User=(event.user || Username || actor.email || 'Unknown') | let Bytes=(ZENTotalBytesTxClient || event.outbytes || 0) | group UploadBytes=sum(Bytes), Events=count(), FirstSeen=min(timestamp), LastSeen=max(timestamp) by User, Dest=(Host || host || event.url || event.hostname), Category=(event.urlcategory || urlcategory), App=(event.appname || Application) | filter UploadBytes > 0 | sort -UploadBytes | nolimit
  merge:
    kind: aggregate
    key_cols:
    - User
    - Dest
    - Category
    - App
    sum_cols:
    - UploadBytes
    - Events
    min_cols:
    - FirstSeen
    max_cols:
    - LastSeen
  scope: subject
- id: zia-bp-02-2-2
  title: Disable Events + Blocked Activity Timeline
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union (serverHost='{{src_zia|zia}}' event.action contains:anycase('block') | columns timestamp,Type='Blocked Web Event'), (serverHost='{{src_zia_disable_reason|zia_disable_reason}}' | columns timestamp,Type='ZIA Disabled') | group count=count() by timestamp=timebucket('1h'),Type | transpose Type
  merge:
    kind: aggregate
    key_cols:
    - timestamp
    - Type
    sum_cols:
    - count
  scope: subject
- id: domain-url-egress
  title: Domain / URL -> Egress
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.hostname contains:anycase('{{domain}}') OR event.url contains:anycase('{{domain}}') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | columns timestamp,event.user,event.devicehostname,event.hostname,event.url,event.appname,event.action,event.requestmethod,outB,event.ClientIP,event.useragent | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: cov-06-2-2
  title: EDR Coverage Calendar — by Agent UUID
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: '| filter agent.uuid == ''{{entity}}'' | let ts=(timestamp || event.time || published || sca:ingestTime) | filter ts = * | group Events=count(), EventTypes=array_agg_distinct(event.type, 20), Users=array_agg_distinct(src.process.user, 10) by Day.timestamp=timebucket(ts, ''1d'') | sort -Day.timestamp | nolimit'
  merge:
    kind: aggregate
    key_cols:
    - Day.timestamp
    - '''1d'')'
    sum_cols:
    - Events
    array_cols:
    - EventTypes
    - Users
  scope: subject
- id: cov-05-2-2
  title: EDR Source Coverage — by Username
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: '| filter src.process.user contains:anycase(''{{entity}}'') OR event.login.userName contains:anycase(''{{entity}}'') | let ts=(timestamp || event.time || published || sca:ingestTime) | filter ts = * | group FirstSeen.timestamp=min(ts), LastSeen.timestamp=max(ts), EventCount=count(), Endpoints=array_agg_distinct(endpoint.name, 20), AgentUUIDs=array_agg_distinct(agent.uuid, 20), EventTypes=array_agg_distinct(event.type, 20) by Source=serverHost | sort -EventCount | nolimit'
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
    array_cols:
    - Endpoints
    - AgentUUIDs
    - EventTypes
  scope: subject
- id: opl-04-2
  title: Endpoint Software Inventory
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_oplockdown|oplockdown}}' | filter endpoint.name contains:anycase('S1LT-G35BSW3') | group FirstRecord.timestamp=min(timestamp), LastRecord.timestamp=max(timestamp), AppLastSeen.timestamp=max(lastseen) by endpoint.name, owner, app_name, tag | sort app_name
  merge:
    kind: aggregate
    key_cols:
    - endpoint.name
    - owner
    - app_name
    - tag
    min_cols:
    - FirstRecord.timestamp
    max_cols:
    - LastRecord.timestamp
    - AppLastSeen.timestamp
  scope: subject
- id: endpoint-staging-egress
  title: Endpoint staging → egress
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" Correlate archive creation to upload
  merge:
    kind: append
  scope: subject
- id: zia-dlp-34-2
  title: Executed from Downloads / Temp
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'Process Creation' ) | filter( (tgt.process.image.path || src.process.image.path) contains:anycase('\\Downloads\\','\\Temp\\','\\AppData\\Local\\Temp\\','/Downloads/','/tmp/','/private/tmp/') ) | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc=(tgt.process.name || src.process.name), Path=(tgt.process.image.path || src.process.image.path), Cmd=(tgt.process.cmdline || src.process.cmdline), Parent=(src.process.parent.name || '') | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: file-name-activity
  title: File Name -> Activity
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" message contains:anycase('{{file_or_title}}') OR event.doc_title contains:anycase('{{file_or_title}}') OR entity.file.name contains:anycase('{{file_or_title}}') OR tgt.file.path contains:anycase('{{file_or_title}}') | columns timestamp, serverHost, actor.email, user, event.name, action, event.doc_title, entity.file.name, tgt.file.path, ipAddress | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-11-2
  title: File Sharing Services
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('wetransfer.com','mega.nz','mediafire.com','sendspace.com','transfernow.net','gofile.io','file.io','dropbox.com') | columns ts, event.user, event.deviceowner, event.devicehostname, host, event.url, event.appname, event.outbytes, event.action, event.rulelabel | sort -ts | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-35-2
  title: Files Written to External Device
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'File Creation' AND tgt.file.location == 'External Device' ) | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), File=tgt.file.path, Ext=tgt.file.extension, Size=(tgt.file.size || 0), Proc=(src.process.name || src.process.image.path), Cmd=src.process.cmdline | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-general-pivot-search-01
  title: General Pivot search 01
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: any( actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, email, login, elogin, account, operator, src.process.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, ipAddress, ip, session, event.client_id, event.app_name, event.api_name, id.applicationName, Source\ User ) contains '{{entity}}' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: imported-searches-general-pivot-search-02
  title: General Pivot search 02
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: any( actor.alternateId, actor.email, actor.user.email, actor.email_address, actor, user, user.email, userName, USER_ID, email, login, elogin, account, operator, src.process.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, Source\ User ) contains '{{entity}}'
  merge:
    kind: append
  scope: subject
- id: imported-searches-general-pivot-search-03
  title: General Pivot search 03
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: any( actor.alternateId, actor.email, actor.user.email, actor.email_address, actor, user, user.email, userName, USER_ID, email, login, elogin, account, operator, src.process.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, Source\ User ) contains '{{entity}}' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: imported-searches-general-pivot-search-04
  title: General Pivot search 04
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: any( actor.alternateId, actor.email, actor.user.email, actor.email_address, actor, user, user.email, userName, USER_ID, email, login, elogin, account, operator, src.process.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, "Source\ User" ) contains '{{entity}}' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: imported-searches-general-pivot-search-05
  title: General Pivot search 05
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: any(*email*, *user*, *username*, *userName*, *actor*, *login*, *elogin*, *account*, *principal*, *identity*, *initiated_by*, *created_by*, *operator*, *Source User*, *target_user*, *sessionIssuer*, *owner*, *requester*, *caller*, *member*, *employee*) contains '{{entity}}' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: imported-searches-general-pivot-search-06
  title: General Pivot search 06
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: any(actor.alternateId, actor.email, actor.user.email, actor, user, userName, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, src.process.user, USER_ID, created_by.login, created_by.name, login, elogin, Source User, target_user.email, user.email, email, operator, actors[].summary, initiated_by.email, initiated_by.username, content.email, account, User, actor.email_address) contains '{{entity}}' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: imported-searches-general-pivot-search-07
  title: General Pivot search 07
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: any(actor.alternateId, actor.email, actor.user.email, actor, user, userName, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, src.process.user, USER_ID, created_by.login, created_by.name, login, elogin, Source User, target_user.email, user.email, email, operator, "actors[].summary", initiated_by.email, initiated_by.username, content.email, account, User, actor.email_address) contains '{{entity}}' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: imported-searches-general-pivot-search-08
  title: General Pivot search 08
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: any(actor.alternateId, actor.email, actor.user.email, actor, user, userName, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, src.process.user, USER_ID, created_by.login, created_by.name, login, elogin, "Source User", target_user.email, user.email, email, operator, "actors[].summary", initiated_by.email, initiated_by.username, content.email, account, User, actor.email_address) contains '{{entity}}' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: git-01-2
  title: GitHub Identity Coverage
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: serverHost in ('ghe_server_prod','ghe_server_dev','github_org_auth','github_s3') | filter actor contains:anycase('{{entity}}') OR user contains:anycase('{{entity}}') OR actor.email contains:anycase('{{entity}}') | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp), Actions=array_agg_distinct(action, 20), Repos=array_agg_distinct(repo, 20) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
    array_cols:
    - Actions
    - Repos
  scope: subject
- id: google-confidential-external-sharing
  title: Google Confidential External Sharing
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost == 'google_workspace' | filter id.applicationName == 'drive' | filter classificationLabel in:anycase ('Highly Confidential','Confidential') | filter event.visibility in:anycase ('shared_externally','anyone_with_link','people_with_link','public_on_the_web','people_within_domain') OR event.old_visibility in:anycase ('shared_externally','anyone_with_link','people_with_link','public_on_the_web') | columns timestamp, actor.email, event.owner, event.doc_title, event.doc_type, classificationLabel, event.name, event.visibility, event.old_visibility, ipAddress, networkInfo.regionCode, networkInfo.ipAsn, event.doc_id | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-google-workspace-search-43
  title: Google Workspace search 43
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost == "google_workspace" | filter id.applicationName == "drive" | filter classificationLabel in:anycase ("Highly Confidential","Confidential") | filter event.visibility in:anycase ("shared_externally","anyone_with_link","people_with_link","public_on_the_web") OR event.old_visibility in:anycase ("shared_externally","anyone_with_link","people_with_link","public_on_the_web") | group events=count(), docs=estimate_distinct(event.doc_id), titles=array_agg_distinct(event.doc_title,20), visibilities=array_agg_distinct(event.visibility,10), ips=array_agg_distinct(ipAddress,20) by actor.email | sort -events | nolimit
  merge:
    kind: aggregate
    key_cols:
    - actor.email
    sum_cols:
    - events
    approx_cols:
    - docs
    array_cols:
    - titles
    - visibilities
    - ips
  scope: subject
- id: imported-searches-google-workspace-search-44
  title: Google Workspace search 44
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost == "google_workspace" | filter id.applicationName == "drive" | filter classificationLabel in:anycase ("Highly Confidential","Confidential") | filter event.visibility in:anycase ("shared_externally","anyone_with_link","people_with_link","public_on_the_web") OR event.old_visibility in:anycase ("shared_externally","anyone_with_link","people_with_link","public_on_the_web") | group events=count(), docs=estimate_distinct(event.doc_id), actors=array_agg_distinct(actor.email,20), titles=array_agg_distinct(event.doc_title,20) by event.owner | sort -events | nolimit
  merge:
    kind: aggregate
    key_cols:
    - event.owner
    sum_cols:
    - events
    approx_cols:
    - docs
    array_cols:
    - actors
    - titles
  scope: subject
- id: imported-searches-google-workspace-search-45
  title: Google Workspace search 45
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost == "google_workspace" | filter id.applicationName == "drive" | filter classificationLabel in:anycase ("Highly Confidential","Confidential") | filter event.visibility in:anycase ("shared_externally","anyone_with_link","people_with_link","public_on_the_web") OR event.old_visibility in:anycase ("shared_externally","anyone_with_link","people_with_link","public_on_the_web") | columns timestamp, actor.email, event.owner, event.doc_title, event.doc_type, classificationLabel, event.name, event.visibility, event.old_visibility, ipAddress, networkInfo.regionCode, networkInfo.ipAsn, event.doc_id | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-google-workspace-search-46
  title: Google Workspace search 46
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost == "google_workspace" | filter id.applicationName == "drive" | filter classificationLabel in:anycase ("Highly Confidential","Confidential") | filter event.visibility in:anycase ("shared_externally","anyone_with_link","people_with_link","public_on_the_web","people_within_domain") OR event.old_visibility in:anycase ("shared_externally","anyone_with_link","people_with_link","public_on_the_web") | columns timestamp, actor.email, event.owner, event.doc_title, event.doc_type, classificationLabel, event.name, event.visibility, event.old_visibility, ipAddress, networkInfo.regionCode, networkInfo.ipAsn, event.doc_id | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-google-workspace-search-61
  title: Google Workspace search 61
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_google_workspace|google_workspace}}' | filter event.name in:anycase('download','copy','print','trash','delete','rename','access_item_content','sync_item_content','view','change_document_visibility','change_document_access_scope') | group events=count(), uniq_ips=estimate_distinct(ipAddress), actions=array_agg_distinct(event.name,20), apps=array_agg_distinct(actor.applicationInfo.applicationName,10), labels=array_agg_distinct(classificationLabel,10), visibilities=array_agg_distinct(event.visibility,10) by actor.email, event.doc_title, event.doc_id | sort -events | nolimit
  merge:
    kind: aggregate
    key_cols:
    - actor.email
    - event.doc_title
    - event.doc_id
    sum_cols:
    - events
    approx_cols:
    - uniq_ips
    array_cols:
    - actions
    - apps
    - labels
    - visibilities
  scope: subject
- id: imported-searches-google-workspace-search-69
  title: Google Workspace search 69
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_google_workspace|google_workspace}}' | filter event.name in:anycase('copy','print','trash','delete','rename') | columns timestamp, actor.email, event.name, event.doc_title, event.doc_id, classificationLabel, ipAddress, actor.applicationInfo.applicationName | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-google-workspace-search-70
  title: Google Workspace search 70
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_google_workspace|google_workspace}}' | filter classificationLabel in:anycase('Highly Confidential','Confidential') | columns timestamp, actor.email, event.name, event.doc_title, event.doc_id, event.doc_type, classificationLabel, event.visibility, ipAddress, networkInfo.regionCode, actor.applicationInfo.applicationName | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-google-workspace-search-71
  title: Google Workspace search 71
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_google_workspace|google_workspace}}' event.name='download' | columns timestamp, actor.email, event.doc_title, event.doc_id, event.doc_type, classificationLabel, event.visibility, ipAddress, networkInfo.regionCode, networkInfo.ipAsn, actor.applicationInfo.applicationName | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: aws-03-2
  title: GuardDuty / SecurityHub Findings by Identity/IP
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: serverHost in ('ow-guardduty','nglz-guardduty','scalyr-guardduty','attivo-guardduty','nglz-securityhub','scalyr-securityhub') | filter record contains:anycase('{{entity}}') OR Service.Action.RemoteIpDetails.ipAddressV4 == '{{ip}}' OR Resources contains:anycase('{{entity}}') | sort -timestamp | nolimit | columns Time=simpledateformat(timestamp,'yyyy-MM-dd HH:mm:ss'), Source=serverHost, Type=Type, Severity=Severity, Account=AccountId, Region=Region, Resource=Resource, RemoteIP=Service.Action.RemoteIpDetails.ipAddressV4
  merge:
    kind: append
  scope: subject
- id: zia-dlp-15-2
  title: High Frequency Uploads in Short Window
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let method=(event.requestmethod || event.method || '') | let url=(event.url || event.hostname || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | filter isUpload | group events=count(), total_out=sum(outB), destinations=estimate_distinct(event.hostname || event.url || event.dns_req) by bucket=timebucket(ts,'15m'), event.user, event.devicehostname | filter events >= 10 | sort -total_out | nolimit
  merge:
    kind: aggregate
    key_cols:
    - bucket
    - '''15m'')'
    - event.user
    - event.devicehostname | filter events >
    sum_cols:
    - events
    - total_out
    approx_cols:
    - destinations
  scope: subject
- id: opl-03-2
  title: High-Risk Software Categories
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_oplockdown|oplockdown}}' | filter tag in ('VPN','RemoteManagement','AI-MISUSE','AI-IDE','Filesharing','Peer-To-Peer','Games') | group Endpoints=estimate_distinct(endpoint.name), Users=estimate_distinct(owner), FirstRecord.timestamp=min(timestamp), LastRecord.timestamp=max(timestamp), AppLastSeen.timestamp=max(lastseen) by app_name, tag | sort -Endpoints
  merge:
    kind: aggregate
    key_cols:
    - app_name
    - tag
    min_cols:
    - FirstRecord.timestamp
    max_cols:
    - LastRecord.timestamp
    - AppLastSeen.timestamp
    approx_cols:
    - Endpoints
    - Users
  scope: subject
- id: zia-dlp-38-2
  title: High-Volume USB Write Summary
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'File Creation' AND tgt.file.location == 'External Device' ) | group Files=count(), TotalBytes=sum(tgt.file.size), Extensions=array_agg_distinct(tgt.file.extension,20) by bucket=timebucket(event.time,'1h'), User=(src.process.user || user.name), endpoint.name | filter( Files >= 10 OR TotalBytes >= 100000000 ) | sort -TotalBytes | nolimit
  merge:
    kind: aggregate
    key_cols:
    - bucket
    - '''1h'')'
    - User
    - endpoint.name | filter( Files >
    sum_cols:
    - Files
    - TotalBytes
    array_cols:
    - Extensions
  scope: subject
- id: zia-dlp-40-2
  title: History Clearing / Terminal Cleanup
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'Process Creation' ) | let Cmd=(src.process.cmdline || tgt.process.cmdline || '') | filter( Cmd contains:anycase('history -c','Clear-History','rm -rf ~/.bash_history','rm -rf ~/.zsh_history','del ConsoleHost_history.txt','Remove-Item ConsoleHost_history.txt') ) | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc=(src.process.name || tgt.process.name), Cmd | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: anti-forensics-history-clearing-terminal-cleanup
  title: History Clearing / Terminal Cleanup
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('Process Creation','Process') | let Cmd=(src.process.cmdline || tgt.process.cmdline || '') | filter Cmd contains:anycase('history -c','Clear-History','rm -rf ~/.bash_history','rm -rf ~/.zsh_history','del ConsoleHost_history.txt','Remove-Item ConsoleHost_history.txt') | columns ts.timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc=(src.process.name || tgt.process.name), Cmd | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: ip-everything
  title: IP -> Everything
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" ip='{{ip}}' OR ipAddress='{{ip}}' OR client.ipAddress='{{ip}}' OR detail.payload.SourceIp='{{ip}}' OR CLIENT_IP='{{ip}}' OR event.src_ip='{{ip}}' OR event.dest_ip='{{ip}}' | columns timestamp, serverHost, actor.email, user, username, detail.payload.Username, ipAddress, client.ipAddress, detail.payload.SourceIp, CLIENT_IP, event.name, event.type | sort -timestamp | nolimit0
  merge:
    kind: append
  scope: subject
- id: net-01-2
  title: IP Pivot Across Network Sources
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost in ('dns-query-logs','panw','pfsense','olympus-vpc-flowlogs','zia','zscaler','aws-network-firewall') | filter ipAddress == '{{ip}}' OR ip == '{{ip}}' OR event.src_ip == '{{ip}}' OR event.dest_ip == '{{ip}}' OR sourceIPAddress == '{{ip}}' OR clientpublicIP == '{{ip}}' OR SourceAddress == '{{ip}}' OR DestinationAddress == '{{ip}}' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp), Hosts=array_agg_distinct(serverHost, 20), Questions=array_agg_distinct(question, 20) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
    array_cols:
    - Hosts
    - Questions
  scope: subject
- id: itm-boundary-policy-minor-violations
  title: ITM Boundary Testing - Minor Policy Violations
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let host=(event.hostname || event.url || event.dns_req || '') | filter event.action in:anycase('Blocked','Denied','Reset','Quarantine') OR event.rulelabel contains:anycase('policy','dlp','blocked','restricted') | columns ts.timestamp=(event.datetime || timestamp || sca:ingestTime), event.user, event.devicehostname, host, event.url, event.appname, event.action, event.rulelabel | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-aws-s3-bulk-access
  title: ITM Collection - AWS S3 Bulk Access
  notes: 'Source: aws | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_aws|aws}}' | filter eventName contains:anycase('GetObject','ListBucket','CopyObject','PutObject') OR event.name contains:anycase('GetObject','ListBucket','CopyObject','PutObject') | group events=count(), buckets=estimate_distinct(requestParameters.bucketName), keys=estimate_distinct(requestParameters.key) by userIdentity.arn, sourceIPAddress, eventName | sort -events | nolimit
  merge:
    kind: aggregate
    key_cols:
    - userIdentity.arn
    - sourceIPAddress
    - eventName
    sum_cols:
    - events
    approx_cols:
    - buckets
    - keys
  scope: subject
- id: itm-archive-creation
  title: ITM Collection - Archive Creation
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('Process Creation','Process Start','Process','File Creation') | let Proc=(src.process.name || tgt.process.name || process.name || '') | let Cmd=(src.process.cmdline || tgt.process.cmdline || process.cmdline || '') | let Path=(tgt.file.path || src.process.image.path || tgt.process.image.path || '') | filter Proc contains:anycase('7z','zip','rar','tar','gzip','powershell','Compress-Archive') OR Cmd contains:anycase('Compress-Archive',' tar ',' 7z ',' zip ',' rar ') OR Path contains:anycase('.zip','.7z','.rar','.tar','.gz') | columns ts.timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc, Cmd, Path | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-github-clone-download
  title: ITM Collection - GitHub Clone / Download Activity
  notes: 'Source: github | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_github|github}}' | filter action contains:anycase('git.clone','repo.download_zip','repo.download_tar','repository_vulnerability_alert','org.oauth_app_authorization') OR event.action contains:anycase('git.clone','download','clone') | columns ts.timestamp=(created_at || timestamp), actor=(actor || actor.email || user), action=(action || event.action), repo=(repo || repository || repo.name), org, ip=(actor_ip || ip) | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-google-drive-download-export
  title: ITM Collection - Google Drive Download / Export
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_google_workspace|google_workspace}}' id.applicationName='drive' | filter event.name contains:anycase('download','export','copy') OR event.type contains:anycase('download','export') | columns ts.timestamp=(id.time || timestamp), actor.email, event.name, event.type, doc_title=(parameters.doc_title || parameters.title || ''), target=(target.email || target.name || ''), owner=(owner.email || ''), ipAddress | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-salesforce-export-report
  title: ITM Collection - Salesforce Export / Report Activity
  notes: 'Source: salesforce | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce|salesforce}}' | filter EVENT_TYPE contains:anycase('ReportExport','BulkApi','Api','DataExport') OR detail.payload.EVENT_TYPE contains:anycase('ReportExport','BulkApi','Api','DataExport') OR event.name contains:anycase('export','report') | columns ts.timestamp=(TIMESTAMP_DERIVED || timestamp), Username=(detail.payload.Username || USER_ID || user), EVENT_TYPE=(detail.payload.EVENT_TYPE || EVENT_TYPE || event.name), CLIENT_IP=(detail.payload.CLIENT_IP || CLIENT_IP), URI=(detail.payload.URI || URI), ROWS=(detail.payload.ROWS_PROCESSED || ROWS_PROCESSED) | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-slack-file-activity
  title: ITM Collection - Slack File Activity
  notes: 'Source: slack | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_slack|slack}}' | filter event.action contains:anycase('file','download','upload','shared') OR action contains:anycase('file','download','upload','shared') | columns ts.timestamp=(event.event_ts || timestamp), user=(actor.user.email || actor.email || user.email || user), action=(event.action || action), file=(file.name || file.id || object.name), channel=(channel.name || channel.id), ip=(context.ip_address || ip) | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-large-outbound-transfers
  title: ITM Data Exfiltration - Large Outbound Transfers
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let host=(event.hostname || event.url || event.dns_req || '') | filter outB > 50000000 | columns ts.timestamp=(event.datetime || timestamp || sca:ingestTime), event.user, event.devicehostname, host, event.url, event.appname, outB, event.action, event.rulelabel | sort -outB | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-paste-public-text-sites
  title: ITM Data Exfiltration - Paste / Public Text Sites
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('pastebin.com','gist.github.com','ghostbin.com','paste.ee','rentry.co','dpaste.com','hastebin.com') | columns ts.timestamp=(event.datetime || timestamp || sca:ingestTime), event.user, event.devicehostname, host, event.url, event.appname, event.outbytes, event.action, event.rulelabel | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-personal-cloud-uploads
  title: ITM Data Exfiltration - Personal Cloud Storage
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let host=(event.hostname || event.url || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter host contains:anycase('drive.google.com','dropbox.com','icloud.com','mega.nz','mediafire.com','box.com','wetransfer.com','swisstransfer.com','gofile.io','file.io') | filter outB > 0 | columns ts.timestamp=(event.datetime || timestamp || sca:ingestTime), event.user, event.devicehostname, host, event.url, event.appname, outB, event.action, event.rulelabel | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-personal-webmail-uploads
  title: ITM Data Exfiltration - Personal Webmail
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let host=(event.hostname || event.url || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter host contains:anycase('mail.google.com','gmail.com','outlook.live.com','hotmail.com','mail.yahoo.com','proton.me','protonmail.com') | filter outB > 0 | columns ts.timestamp=(event.datetime || timestamp || sca:ingestTime), event.user, event.devicehostname, host, event.url, outB, event.action, event.rulelabel | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-google-drive-external-share
  title: ITM Exfiltration - Google Drive External Sharing
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_google_workspace|google_workspace}}' id.applicationName='drive' | filter event.name contains:anycase('change_user_access','change_document_access','acl_change','share') OR event.type contains:anycase('acl','permission') | columns ts.timestamp=(id.time || timestamp), actor.email, event.name, event.type, doc_title=(parameters.doc_title || parameters.title || ''), target=(target.email || parameters.target_user || ''), visibility=(parameters.visibility || parameters.new_value || ''), ipAddress | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-ai-sensitive-prompt-violations
  title: ITM Policy Violation - AI Sensitive Prompt Violations
  notes: 'Source: prompt-security | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_prompt_security|prompt-security}}' | filter log.isValid == true | filter log.violations = * OR log.violationFindingsDetails.Secrets = * OR log.violationFindingsDetails.Sensitive_Data = * OR log.violationFindingsDetails.Harmful_Content_Moderator = * | columns ts.timestamp=(log.timestamp || log.event_time || event_time || timestamp || ReceiveTime), log.user, log.application.name, log.policyName, log.violations, log.violationFindingsDetails.Secrets, log.violationFindingsDetails.Sensitive_Data | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-ai-network-usage
  title: ITM Policy Violation - GenAI Network Usage
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.appname contains:anycase('Claude','Anthropic','Codeium','Windsurf','Glean','OpenAI','ChatGPT','Gemini','Copilot','Perplexity','Cursor') OR event.hostname contains:anycase('claude.ai','anthropic.com','openai.com','chatgpt.com','gemini.google.com','perplexity.ai','cursor.com','cursor.sh','windsurf') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | columns ts.timestamp=(event.datetime || timestamp || sca:ingestTime), event.user, event.devicehostname, event.appname, event.hostname, event.url, outB, event.action | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-okta-auth-anomaly
  title: ITM Unauthorized Access - Okta Authentication Review
  notes: 'Source: okta | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_okta|okta}}' | filter eventType contains:anycase('user.authentication','user.session','system.mfa') OR displayMessage contains:anycase('login','mfa','factor','authentication') | columns ts.timestamp=(published || timestamp), actor.alternateId, eventType, displayMessage, client.ipAddress, client.geographicalContext.country, outcome.result, target.displayName | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-okta-mfa-changes
  title: ITM Unauthorized Access - Okta MFA / Factor Changes
  notes: 'Source: okta | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_okta|okta}}' | filter eventType contains:anycase('user.mfa','factor','system.mfa') OR displayMessage contains:anycase('factor','MFA','enroll','reset','deactivate') | columns ts.timestamp=(published || timestamp), actor.alternateId, eventType, displayMessage, client.ipAddress, outcome.result, target.displayName | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: itm-remote-access-tools
  title: ITM Unauthorized Remote Access Tool Execution
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('Process Creation','Process','Process Start') | let Proc=(process.name || src.process.name || tgt.process.name || '') | let Cmd=(process.cmdline || src.process.cmdline || tgt.process.cmdline || '') | filter Proc contains:anycase('teamviewer','anydesk','rustdesk','screenconnect','connectwise','logmein','bomgar','beyondtrust','splashtop','vnc','tailscale') OR Cmd contains:anycase('teamviewer','anydesk','rustdesk','tailscale','screenconnect','connectwise') | columns ts.timestamp=(event.time || event_time || timestamp || ReceiveTime), User=(user.name || src.process.user || event.login.userName || ''), endpoint.name, agent.uuid, Proc, Cmd | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: opl-07-2
  title: Installed Remote Tool → EDR Execution
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('Process Creation','Process','Process Start') | let Proc=(process.name || src.process.name || tgt.process.name || '') | let Cmd=(process.cmdline || src.process.cmdline || tgt.process.cmdline || '') | filter Proc contains:anycase('{{app_name}}') OR Cmd contains:anycase('{{app_name}}') | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(user.name || src.process.user), Proc, Cmd | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-27-2
  title: Large Archive Creation (>50MB)
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'File Creation' AND tgt.file.extension in:anycase('zip','rar','7z','tar','gz','tgz','pst','mbox') ) | let Size=(tgt.file.size || file.size || 0) | filter( Size > 50000000 ) | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), File=tgt.file.path, Ext=tgt.file.extension, Size, Proc=(src.process.name || src.process.image.path), Cmd=src.process.cmdline | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-14-2
  title: Large Uploads
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter outB >= 50000000 | columns ts, event.user, event.deviceowner, event.devicehostname, event.hostname, event.url, event.appname, outB, event.action, event.rulelabel, event.destcountry | sort -outB | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-up-01-2
  title: Large Uploads >50MB
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter outB > 50000000 | columns timestamp,event.user,event.devicehostname,event.hostname,event.url,event.appname,event.action,outB | sort -outB
  merge:
    kind: append
  scope: subject
- id: cov-07-2-2
  title: Navan Travel Coverage
  notes: 'Source: navan-travel-bookings | Imported/generated for SOC investigation library.'
  pq: serverHost='{{src_navan_travel_bookings|navan-travel-bookings}}' | filter passengers.person.email contains:anycase('{{entity}}') OR loggedAsUserName contains:anycase('{{entity}}') OR booker.email contains:anycase('{{entity}}') | let ts=(timestamp || created || lastModified || sca:ingestTime) | filter ts = * | group FirstSeen.timestamp=min(ts), LastSeen.timestamp=max(ts), EventCount=count(), Purposes=array_agg_distinct(purpose, 10) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
    array_cols:
    - Purposes
  scope: subject
- id: zia-dlp-20-2
  title: New / Low Reputation / Unknown Destinations
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.ipcat contains:anycase('unknown') OR event.urlcategory contains:anycase('unknown') OR event.urlsupercategory contains:anycase('unknown') | group events=count(), users=estimate_distinct(event.user), total_out=sum(event.outbytes || 0) by event.hostname, event.url, event.ipcat, event.urlcategory | sort -total_out | nolimit
  merge:
    kind: aggregate
    key_cols:
    - event.hostname
    - event.url
    - event.ipcat
    - event.urlcategory
    sum_cols:
    - events
    - total_out
    approx_cols:
    - users
  scope: subject
- id: opl-02-2
  title: Newly Observed Software by Endpoint
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_oplockdown|oplockdown}}' | group FirstRecord.timestamp=min(timestamp), LastRecord.timestamp=max(timestamp), AppLastSeen.timestamp=max(lastseen) by app_name, tag, endpoint.name, owner | sort -FirstRecord.timestamp | nolimit
  merge:
    kind: aggregate
    key_cols:
    - app_name
    - tag
    - endpoint.name
    - owner
    min_cols:
    - FirstRecord.timestamp
    max_cols:
    - LastRecord.timestamp
    - AppLastSeen.timestamp
  scope: subject
- id: cov-02-2-2
  title: Okta Source Coverage
  notes: 'Source: okta | Imported/generated for SOC investigation library.'
  pq: serverHost='{{src_okta|okta}}' actor.alternateId='{{entity}}' | let ts=(timestamp || published || event.time || sca:ingestTime) | filter ts = * | group FirstSeen.timestamp=min(ts), LastSeen.timestamp=max(ts), EventCount=count(), Countries=array_agg_distinct(client.geographicalContext.country, 10), IPs=array_agg_distinct(client.ipAddress, 10) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
    array_cols:
    - Countries
    - IPs
  scope: subject
- id: corr-01
  title: OpLockdown Remote Tool Installed then EDR Execution
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: 'any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, ''Source\User'') contains "{{entity}}" Step 1: run OPL-03 or OPL-04 to identify app_name and endpoint.name. Step 2: run OPL-07 with {{app_name}}. Step 3: pivot endpoint.name / agent.uuid to process tree and network connections.'
  merge:
    kind: append
  scope: subject
- id: oplockdown-app-edr-execution
  title: OpLockdown app → EDR execution
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" Prove installed app actually ran
  merge:
    kind: append
  scope: subject
- id: oplockdown-app-endpoints
  title: OpLockdown app → endpoints
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" Find where a risky app is installed
  merge:
    kind: append
  scope: subject
- id: opl-ref
  title: OpLockdown vs EDR Interpretation
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: 'any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, ''Source\User'') contains "{{entity}}" oplockdown fields: owner, endpoint.name, app_name, tag, type, lastseen. Treat as software inventory / policy classification. Do not treat as proof of execution. Use EDR to prove runtime behavior.'
  merge:
    kind: append
  scope: subject
- id: opl-06-2-2
  title: Orphaned / Unknown Owner Software
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_oplockdown|oplockdown}}' | filter owner == '-' OR !owner OR owner == '' | group Endpoints=estimate_distinct(endpoint.name), FirstRecord.timestamp=min(timestamp), LastRecord.timestamp=max(timestamp), AppLastSeen.timestamp=max(lastseen) by app_name, tag, endpoint.name, owner | sort -AppLastSeen.timestamp | nolimit
  merge:
    kind: aggregate
    key_cols:
    - app_name
    - tag
    - endpoint.name
    - owner
    min_cols:
    - FirstRecord.timestamp
    max_cols:
    - LastRecord.timestamp
    - AppLastSeen.timestamp
    approx_cols:
    - Endpoints
  scope: subject
- id: zia-dlp-13-2
  title: Paste / Public Text Dump Sites
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('pastebin.com','gist.github.com','ghostbin.com','paste.ee','rentry.co','dpaste.com','hastebin.com') | columns ts, event.user, event.deviceowner, event.devicehostname, host, event.url, event.appname, event.outbytes, event.action, event.rulelabel | sort -ts | nolimit
  merge:
    kind: append
  scope: subject
- id: shadow-it-webmail-file-sharing-paste-public-text-dump-sites
  title: Paste / Public Text Dump Sites
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('pastebin.com','gist.github.com','ghostbin.com','paste.ee','rentry.co','dpaste.com','hastebin.com') | columns ts.timestamp=ts, event.user, event.deviceowner, event.devicehostname, host, event.url, event.appname, event.outbytes, event.action, event.rulelabel | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-10-2
  title: Personal Cloud Storage
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('drive.google.com','dropbox.com','dropboxapi.com','icloud.com','mega.nz','mediafire.com','box.com') | columns ts, event.user, event.deviceowner, event.devicehostname, host, event.url, event.appname, event.outbytes, event.action, event.rulelabel | sort -ts | nolimit
  merge:
    kind: append
  scope: subject
- id: shadow-it-webmail-file-sharing-personal-cloud-storage
  title: Personal Cloud Storage
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('drive.google.com','dropbox.com','dropboxapi.com','icloud.com','mega.nz','mediafire.com','box.com','wetransfer.com','swisstransfer.com') | columns ts.timestamp=ts, event.user, event.deviceowner, event.devicehostname, host, event.url, event.appname, event.outbytes, event.action, event.rulelabel | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: shadow-it-webmail-file-sharing-personal-webmail-activity
  title: Personal Webmail Activity
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('mail.google.com','gmail.com','outlook.live.com','hotmail.com','mail.yahoo.com','proton.me','protonmail.com') | columns ts.timestamp=ts, event.user, event.deviceowner, event.devicehostname, host, event.url, event.outbytes, event.action, event.rulelabel | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-sh-02-2
  title: Personal Webmail Uploads
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.hostname contains:anycase('gmail','outlook.live','hotmail','yahoo','protonmail') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter outB > 1000000 | columns timestamp,event.user,event.hostname,event.url,event.appname,event.action,outB | sort -outB
  merge:
    kind: append
  scope: subject
- id: zia-dlp-23-2
  title: Policy Rules Generating Upload Resets / Blocks
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let method=(event.requestmethod || event.method || '') | let url=(event.url || event.hostname || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | filter isUpload AND !(event.action in ('Allow','Allowed','ALLOW')) | group events=count(), users=estimate_distinct(event.user), total_out=sum(outB) by event.rulelabel, event.action | sort -events | nolimit
  merge:
    kind: aggregate
    key_cols:
    - event.rulelabel
    - event.action
    sum_cols:
    - events
    - total_out
    approx_cols:
    - users
  scope: subject
- id: potential-block-candidates-potential-block-candidates-by-app-destinati
  title: Potential Block Candidates by App / Destination
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let host=(event.hostname || event.url || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter host contains:anycase('swisstransfer','wetransfer','archive.org','mega','signal','telegram','icloud','mediafire','gofile','file.io','pastebin','gist.github','rentry') OR event.appname contains:anycase('uTorrent','BitTorrent','Transmission','AnyDesk','RustDesk','TeamViewer','LogMeIn','Tailscale','NordVPN','ProtonVPN') | group upload_bytes=sum(outB), users=estimate_distinct(event.user), events=count() by host, event.appname, event.action, event.urlcategory | sort -upload_bytes | nolimit
  merge:
    kind: aggregate
    key_cols:
    - host
    - event.appname
    - event.action
    - event.urlcategory
    sum_cols:
    - upload_bytes
    - events
    approx_cols:
    - users
  scope: subject
- id: zia-dlp-42-2
  title: Potential Security Control Tampering
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'Behavioral Indicators' OR event.type == 'Process Creation' ) | let Proc=(src.process.name || tgt.process.name || '') | let Cmd=(src.process.cmdline || tgt.process.cmdline || '') | filter( indicator.name in:anycase('ImpairDefense','AntiVirusEvasion','EventTampering','EtwTampering','DisablingSecurityTools','DisableSecurityTool','ModifyFirewallConfiguration') OR (Proc contains:anycase('sc.exe','reg.exe','powershell.exe') AND Cmd contains:anycase('stop','disable','Set-MpPreference','netsh advfirewall set allprofiles state off')) ) | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc, Cmd, indicator.name, indicator.description | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: anti-forensics-potential-security-control-tampering
  title: Potential Security Control Tampering
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('Behavioral Indicators','Process Creation','Process') | let Proc=(src.process.name || tgt.process.name || '') | let Cmd=(src.process.cmdline || tgt.process.cmdline || '') | filter indicator.name in:anycase('ImpairDefense','AntiVirusEvasion','EventTampering','EtwTampering','DisablingSecurityTools','DisableSecurityTool','ModifyFirewallConfiguration') OR (Proc contains:anycase('sc.exe','reg.exe','powershell.exe') AND Cmd contains:anycase('stop','disable','Set-MpPreference','netsh advfirewall set allprofiles state off')) | columns ts.timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc, Cmd, indicator.name, indicator.description | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-25-2
  title: Potential Staging Behavior
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | group events=count(), small_uploads=count(if(outB > 1000000 AND outB < 10000000,1,null)), large_uploads=count(if(outB >= 10000000,1,null)), total_out=sum(outB) by bucket=timebucket(ts,'1h'), event.user, event.devicehostname | filter small_uploads >= 3 AND large_uploads >= 1 | sort -total_out | nolimit
  merge:
    kind: aggregate
    key_cols:
    - bucket
    - '''1h'')'
    - event.user
    - event.devicehostname | filter small_uploads >
    sum_cols:
    - events
    - small_uploads
    - large_uploads
    - total_out
  scope: subject
- id: zia-dlp-01-2
  title: Potential Upload / Egress Activity
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let method=(event.requestmethod || event.method || '') | let url=(event.url || event.hostname || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | filter isUpload | group count()
  merge:
    kind: append
  scope: subject
- id: zia-dlp-05-2
  title: Potential Upload Activity Over Time
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let method=(event.requestmethod || event.method || '') | let url=(event.url || event.hostname || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | filter isUpload | group events=count(), users=estimate_distinct(event.user) by timestamp=timebucket(ts,'1d') | sort -timestamp
  merge:
    kind: aggregate
    key_cols:
    - timestamp
    - '''1d'')'
    sum_cols:
    - events
    approx_cols:
    - users
  scope: subject
- id: zia-ic-01-2
  title: Potential iCloud Uploads
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.appname='iCloud' OR event.hostname contains:anycase('icloud') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter event.requestmethod in ('POST','PUT') OR outB > 5000000 | columns timestamp,event.user,event.devicehostname,event.hostname,event.url,event.requestmethod,event.action,outB | sort -outB
  merge:
    kind: append
  scope: subject
- id: ai-genai-hunting-prompt-security-violations
  title: Prompt Security Violations
  notes: 'Source: prompt-security | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_prompt_security|prompt-security}}' | filter log.isValid == true | let ts=(log.timestamp || log.event_time || event_time || timestamp || ReceiveTime) | filter log.violations = * OR log.violationFindingsDetails.Secrets = * OR log.violationFindingsDetails.Sensitive_Data = * OR log.violationFindingsDetails.Harmful_Content_Moderator = * | group violations=count(), users=estimate_distinct(log.user) by log.application.name, log.policyName | sort -violations | nolimit
  merge:
    kind: aggregate
    key_cols:
    - log.application.name
    - log.policyName
    sum_cols:
    - violations
    approx_cols:
    - users
  scope: subject
- id: zia-dlp-07-2
  title: Raw Upload / POST Activity
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let method=(event.requestmethod || event.method || '') | let url=(event.url || event.hostname || event.dns_req || '') | let host=(event.hostname || event.url || event.dns_req || '') | let file=(event.filename || event.file_name || event.uploadedfilename || '') | let filetype=(event.filetype || event.contenttype || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | filter isUpload | columns ts, event.user, event.deviceowner, event.devicehostname, method, url, host, event.appname, file, filetype, outB, event.action, event.rulelabel, event.urlcategory, event.destcountry | sort -ts | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-31-2
  title: Rclone Execution
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'Process Creation' OR event.type == 'Process Start' OR dataSource.category == 'process' ) | let Proc=(src.process.name || tgt.process.name || process.name || '') | let Cmd=(src.process.cmdline || tgt.process.cmdline || process.cmdline || '') | filter( Proc contains:anycase('rclone') OR Cmd contains:anycase(' rclone ') ) | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc, Cmd, Parent=(src.process.parent.name || ''), Path=(src.process.image.path || tgt.process.image.path || '') | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: endpoint-exfil-tooling-rclone-execution
  title: Rclone Execution
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('Process Creation','Process Start','Process') | let Proc=(src.process.name || tgt.process.name || process.name || '') | let Cmd=(src.process.cmdline || tgt.process.cmdline || process.cmdline || '') | filter Proc contains:anycase('rclone') OR Cmd contains:anycase(' rclone ') | columns ts.timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc, Cmd, Parent=(src.process.parent.name || ''), Path=(src.process.image.path || tgt.process.image.path || '') | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-it-01-2
  title: Recommended Block Candidates
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.hostname contains:anycase('swisstransfer','wetransfer','archive.org','mega','signal','telegram','icloud','mediafire') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | group upload_bytes=sum(outB), users=estimate_distinct(event.user), events=count() by event.hostname,event.appname,event.action | sort -upload_bytes
  merge:
    kind: aggregate
    key_cols:
    - event.hostname
    - event.appname
    - event.action
    sum_cols:
    - upload_bytes
    - events
    approx_cols:
    - users
  scope: subject
- id: remote-access-vpn-p2p-remote-access-vpn-p2p-executed
  title: Remote Access / VPN / P2P Executed
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('Process Creation','Process','Process Start') | let ts=(event.time || event_time || timestamp || ReceiveTime) | let User=(user.name || src.process.user || event.login.userName || '') | let Proc=(process.name || src.process.name || tgt.process.name || '') | let Cmd=(process.cmdline || src.process.cmdline || tgt.process.cmdline || '') | filter Proc contains:anycase('teamviewer','anydesk','rustdesk','screenconnect','connectwise','logmein','bomgar','beyondtrust','splashtop','vnc','tailscale','openvpn','wireguard','nordvpn','expressvpn','protonvpn','surfshark','utorrent','qbittorrent','transmission') OR Cmd contains:anycase('teamviewer','anydesk','rustdesk','tailscale','openvpn','wireguard','nordvpn','utorrent','qbittorrent','transmission') | columns ts.timestamp=ts, User, endpoint.name, agent.uuid, Proc, Cmd | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: remote-access-vpn-p2p-remote-access-vpn-p2p-installed
  title: Remote Access / VPN / P2P Installed
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_oplockdown|oplockdown}}' | filter tag in ('VPN','RemoteManagement','Peer-To-Peer','Games') | group Endpoints=estimate_distinct(endpoint.name), Users=estimate_distinct(owner), LastSeen.timestamp=max(lastseen) by app_name, tag | sort -Endpoints | nolimit
  merge:
    kind: aggregate
    key_cols:
    - app_name
    - tag
    max_cols:
    - LastSeen.timestamp
    approx_cols:
    - Endpoints
    - Users
  scope: subject
- id: sfd-07-2
  title: Salesforce Session Pivot
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost in ('salesforce-realtime','salesforce_event') | filter detail.payload.LoginKey='{{login_key}}' OR detail.payload.SessionKey='{{session}}' OR LOGIN_KEY='{{login_key}}' OR SESSION_KEY='{{session}}' | let ts=(detail.payload.EventDate || TIMESTAMP_DERIVED || time || timestamp || sca:ingestTime) | columns Time=simpledateformat(ts,'yyyy-MM-dd HH:mm:ss'), Source=serverHost, DetailType=detail-type, EventType=EVENT_TYPE, Username=detail.payload.Username, UserId=detail.payload.UserId, Operation=detail.payload.Operation, EventName=detail.payload.Name, Entity=ENTITY_NAME, URI=URI, IP=detail.payload.SourceIp, ClientIP=CLIENT_IP, Query=detail.payload.Query | sort Time | nolimit0
  merge:
    kind: append
  scope: subject
- id: sfd-01-2
  title: Salesforce Source Coverage
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: serverHost in ('salesforce-realtime','salesforce_event','salesforce_audit') | filter any(detail.payload.Username, detail.payload.UserId, USER_ID, USER_ID_DERIVED, username, CreatedById, account) contains '{{entity}}' OR any(detail.payload.Username, username) contains:anycase('{{entity}}') OR any(detail.payload.UserId, USER_ID, USER_ID_DERIVED, CreatedById) contains '{{sf_user_id}}' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Source=serverHost | sort -EventCount
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: imported-searches-salesforce-search-28
  title: Salesforce search 28
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost in ('salesforce_event','salesforce-realtime') message contains '0056900000BLz4EAAT' message contains:anycase ('Account','Opportunity','Contact','Lead','Case','Report') | parse '""EntityName"":""$entity$""' from message | let ts=(timestamp || sca:ingestTime) | filter ts = * | group EventCount=count() by Day=timebucket(ts,'1d'), entity | sort -Day | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Day
    - '''1d'')'
    - entity
    sum_cols:
    - EventCount
  scope: subject
- id: imported-searches-salesforce-search-29
  title: Salesforce search 29
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost in ('salesforce_event','salesforce-realtime') message contains '0056900000BLz4EAAT' message matches:anycase ('ReportEventStream','ApexExportEventStream','BulkApiResultEventStream','ListViewEventStream') | parse '""detail-type"":""$detailtype$""' from message | columns timestamp, detailtype, message | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-salesforce-search-39
  title: Salesforce search 39
  notes: 'Source: salesforce-realtime | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_realtime|salesforce-realtime}}' detail-type='LoginAsEventStream' | columns time, detail.payload.Username, detail.payload.DelegatedUsername, detail.payload.LoginAsCategory, detail.payload.SourceIp, detail.payload.Country, detail.payload.City | sort -time | nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-salesforce-search-40
  title: Salesforce search 40
  notes: 'Source: salesforce-realtime | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_realtime|salesforce-realtime}}' detail-type='LoginEventStream' detail.payload.LoginType=* | group ct=count() by detail.payload.LoginType | sort -ct
  merge:
    kind: aggregate
    key_cols:
    - detail.payload.LoginType
    sum_cols:
    - ct
  scope: subject
- id: imported-searches-salesforce-search-41
  title: Salesforce search 41
  notes: 'Source: salesforce-realtime | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_realtime|salesforce-realtime}}' detail-type='LoginEventStream' | group users=estimate_distinct(detail.payload.Username) by timestamp=timebucket('1h')
  merge:
    kind: aggregate
    key_cols:
    - timestamp
    approx_cols:
    - users
  scope: subject
- id: imported-searches-salesforce-search-42
  title: Salesforce search 42
  notes: 'Source: salesforce-realtime | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_realtime|salesforce-realtime}}' detail-type='LoginEventStream' | group ct=count() by timestamp=timebucket('1h')
  merge:
    kind: aggregate
    key_cols:
    - timestamp
    sum_cols:
    - ct
  scope: subject
- id: imported-searches-salesforce-search-47
  title: Salesforce search 47
  notes: 'Source: salesforce_event | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_event|salesforce_event}}' message contains '0056900000BLz4EAAT' message contains:anycase ('PermissionSet','Profile','setup_audit_trail') | columns timestamp, message | sort -timestamp | nolimit0
  merge:
    kind: append
  scope: subject
- id: imported-searches-salesforce-search-48
  title: Salesforce search 48
  notes: 'Source: salesforce_event | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_event|salesforce_event}}' message contains ('0056900000BLz4EAAT','ApiEventStream') | parse '"Method":"$method$"' from message | parse '"Entity":"$entity$"' from message | parse '"ClientId":"$clientid$"' from message | group EventCount=count() by method, entity, clientid | sort -EventCount | nolimit0
  merge:
    kind: aggregate
    key_cols:
    - method
    - entity
    - clientid
    sum_cols:
    - EventCount
  scope: subject
- id: imported-searches-salesforce-search-49
  title: Salesforce search 49
  notes: 'Source: salesforce_event | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_event|salesforce_event}}' message contains '0056900000BLz4EAAT' message matches ('ReportEventStream','BulkApiResultEventStream','ListViewEventStream','ApexExportEventStream') | parse '"detail-type":"$detailtype$"' from message | columns timestamp, detailtype, message | sort -timestamp | nolimit0
  merge:
    kind: append
  scope: subject
- id: imported-searches-salesforce-search-50
  title: Salesforce search 50
  notes: 'Source: salesforce_event | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_event|salesforce_event}}' message contains ('0056900000BLz4EAAT','LoginEventStream') | parse '"LoginType":"$logintype$"' from message | parse '"SourceIp":"$srcip$"' from message | parse '"City":"$city$"' from message | parse '"Country":"$country$"' from message | parse '"Application":"$app$"' from message | columns timestamp, logintype, srcip, city, country, app | sort -timestamp | nolimit0
  merge:
    kind: append
  scope: subject
- id: imported-searches-salesforce-search-51
  title: Salesforce search 51
  notes: 'Source: salesforce_event | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_event|salesforce_event}}' message contains '0056900000BLz4EAAT' | parse '"detail-type":"$detailtype$"' from message | let ts=(timestamp || sca:ingestTime) | filter ts = * | group EventCount=count() by Day=timebucket(ts,'1d'), detailtype | sort -Day | nolimit0
  merge:
    kind: aggregate
    key_cols:
    - Day
    - '''1d'')'
    - detailtype
    sum_cols:
    - EventCount
  scope: subject
- id: imported-searches-salesforce-search-52
  title: Salesforce search 52
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost in ('salesforce_event','salesforce-realtime') message contains '0056900000BLz4EAAT' | group EventCount=count(), FirstSeen.timestamp=min(timestamp || sca:ingestTime), LastSeen.timestamp=max(timestamp || sca:ingestTime) by Source=serverHost | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: imported-searches-salesforce-search-72
  title: Salesforce search 72
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost in ('salesforce_event','salesforce-realtime') | let User=(USER_NAME || detail.payload.Username) | let Entity=(ENTITY_NAME || detail.payload.QueriedEntities) | let IP=(CLIENT_IP || detail.payload.SourceIp) | group events=count(), ips=array_agg_distinct(IP,10), apps=array_agg_distinct((CONNECTED_APP_NAME || detail.payload.Application),10), eventTypes=array_agg_distinct((EVENT_TYPE || detail-type),20) by User, Entity | sort -events | nolimit
  merge:
    kind: aggregate
    key_cols:
    - User
    - Entity
    sum_cols:
    - events
    array_cols:
    - ips
    - apps
    - eventTypes
  scope: subject
- id: imported-searches-salesforce-search-73
  title: Salesforce search 73
  notes: 'Source: salesforce-realtime | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_realtime|salesforce-realtime}}' detail-type='FileEvent' | columns time, detail.payload.Username, detail.payload.FileAction, detail.payload.FileName, detail.payload.FileType, detail.payload.ContentSize, detail.payload.DocumentId, detail.payload.VersionId, detail.payload.SourceIp | sort -time | nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-salesforce-search-74
  title: Salesforce search 74
  notes: 'Source: salesforce-realtime | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_realtime|salesforce-realtime}}' | filter detail-type='BulkApiResultEvent' OR detail.payload.ApiType=* OR detail.payload.RowsProcessed > 0 | columns time, detail.payload.Username, detail-type, detail.payload.ApiType, detail.payload.Operation, detail.payload.RowsProcessed, detail.payload.Status, detail.payload.SourceIp, detail.payload.Application, detail.payload.Query | sort -time | nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-salesforce-search-75
  title: Salesforce search 75
  notes: 'Source: salesforce_event | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_event|salesforce_event}}' USER_NAME=* API_CLIENT_CATEGORY='EXTERNAL_APPLICATION' | columns TIMESTAMP_DERIVED, USER_NAME, CONNECTED_APP_NAME, API_FAMILY, API_RESOURCE, ENTITY_NAME, CLIENT_IP, STATUS_CODE | sort -TIMESTAMP_DERIVED | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-44-2
  title: Sensitive Activity + Exfil Tooling
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union ( serverHost == 'google_workspace' | filter classificationLabel in:anycase('Highly Confidential','Confidential') | filter event.name in:anycase('download','sync_item_content','access_item_content','copy','print') | let user_name=(actor.email || event.owner || '') | let signal='sensitive_access' | columns timestamp=(timestamp || id.time || event.time), user_name, signal, object=(event.doc_title || event.doc_id || '-'), detail=classificationLabel ), ( | filter( event.type == 'Process Creation' ) | let user_name=(src.process.user || user.name || '') | let Proc=(src.process.name || tgt.process.name || '') | let Cmd=(src.process.cmdline || tgt.process.cmdline || '') | filter( Proc contains:anycase('rclone','scp','sftp','rsync','curl','wget','aws','gsutil','megacmd','megasync','onedrive','dropbox','drivefs','box','icloud') OR Cmd contains:anycase(' rclone ',' scp ',' sftp ',' rsync ',' curl ',' wget ',' aws s3 ',' gsutil ',' mega-put ',' mega-cp ') ) | let signal='exfil_tooling' | columns timestamp=(event.time || timestamp), user_name, signal, object=(Proc || '-'), detail=Cmd ) | sort user_name, timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-45-2
  title: Sensitive Activity + ZIA Egress
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union ( serverHost == 'google_workspace' | filter classificationLabel in:anycase('Highly Confidential','Confidential') | filter event.name in:anycase('download','sync_item_content','access_item_content','copy','print') | let user_name=(actor.email || event.owner || '') | let signal='sensitive_access' | columns timestamp=(timestamp || id.time || event.time), user_name, signal, object=(event.doc_title || event.doc_id || '-'), detail=(event.name || classificationLabel) ), ( serverHost == 'zia' | let user_name=(event.user || actor.email || '') | let url=(event.url || event.hostname || event.dns_req || '-') | let method=(event.requestmethod || event.method || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | filter isUpload | let signal='zia_egress' | columns timestamp=(event.datetime || timestamp || sca:ingestTime), user_name, signal, object=url, detail=(event.action || host) ) | sort user_name, timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-43-2
  title: Sensitive Cloud Activity + Endpoint Staging Timeline
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union ( serverHost == 'google_workspace' | filter classificationLabel in:anycase('Highly Confidential','Confidential') | filter event.name in:anycase('download','sync_item_content','access_item_content','view','copy','print','change_document_visibility','change_document_access_scope') | let user_name=(actor.email || event.owner || '') | let signal='workspace_sensitive_activity' | let object=(event.doc_title || event.doc_id || '-') | columns timestamp=(timestamp || id.time || event.time), user_name, signal, object, detail=(event.name || event.visibility || classificationLabel), endpoint='-', agent='-' ), ( serverHost == 'box' | filter event_type matches '(?i).*DOWNLOAD.*|.*UPLOAD.*|.*SHARED_LINK.*|.*COLLABORATION.*' | let user_name=(action_by.login || created_by.login || '') | let signal='box_file_activity' | let object=(source.item_name || source.item_id || '-') | columns timestamp=(created_at || timestamp), user_name, signal, object, detail=event_type, endpoint='-', agent='-' ), ( serverHost == 'onedrive' | filter operation matches '(?i).*Download.*|.*Upload.*|.*Sharing.*|.*AnonymousLink.*|.*SecureLink.*|.*Permission.*' | let user_name=(auditData.UserId || userPrincipalName || '') | let signal='onedrive_file_activity' | let object=(auditData.SourceFileName || auditData.ObjectId || '-') | columns timestamp=(timestamp || event.time), user_name, signal, object, detail=operation, endpoint='-', agent='-' ), ( | filter( event.type == 'File Creation' AND (tgt.file.extension in:anycase('zip','rar','7z','tar','gz','tgz','pst','mbox') OR tgt.file.path matches '(?i).*\\\\Users\\\\.*\\\\(Downloads|Desktop|Documents)\\\\.*' OR tgt.file.path matches '(?i).*/(Downloads|Desktop|Documents|tmp|Temp)/.*') ) | let user_name=(src.process.user || user.name || '') | let signal='endpoint_staging' | let object=(tgt.file.path || '-') | columns timestamp=(event.time || timestamp), user_name, signal, object, detail=(tgt.file.extension || src.process.name), endpoint=endpoint.name, agent=agent.uuid ) | sort user_name, timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-28-2
  title: Sensitive Extensions Created in User Staging Paths
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'File Creation' ) | filter( tgt.file.path matches '(?i).*\\\\Users\\\\.*\\\\(Downloads|Desktop|Documents)\\\\.*' OR tgt.file.path matches '(?i).*\\\\AppData\\\\(Local|Roaming)\\\\.*\\\\Temp\\\\.*' OR tgt.file.path matches '(?i).*/(Downloads|Desktop|Documents|tmp|Temp)/.*' ) | filter( tgt.file.extension in:anycase('go','rs','py','js','ts','c','cpp','h','java','cs','kt','swift','sql','env','pem','key','p12','pfx','kdbx','tf','yml','yaml','json','csv','xlsx','xls','docx','pptx','pdf') ) | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), File=tgt.file.path, Ext=tgt.file.extension, Size=tgt.file.size, Proc=(src.process.name || src.process.image.path), Cmd=src.process.cmdline | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: session-timeline
  title: Session -> Timeline
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" session='{{session}}' OR authenticationContext.externalSessionId='{{session}}' OR detail.payload.SessionKey='{{session}}' OR SESSION_KEY='{{session}}' OR detail.payload.LoginKey='{{login_key}}' OR LOGIN_KEY='{{login_key}}' | columns timestamp, serverHost, actor.email, detail.payload.Username, event.name, event.type, action, detail-type, ipAddress, detail.payload.SourceIp, CLIENT_IP | sort timestamp | nolimit0
  merge:
    kind: append
  scope: subject
- id: imported-searches-slack-search-65
  title: Slack search 65
  notes: 'Source: slack | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_slack|slack}}' action in:anycase('file_downloaded','file_shared','file_uploaded','file_deleted') context.app.name != 'Glean' context.ua != 'GleanSearchCrawlerBot' !(context.ua contains:anycase 'Scio Search') !(context.app.name contains:anycase 'Scio') | group events=count(), uniq_ips=estimate_distinct(context.ip_address), actions=array_agg_distinct(action,20) by actor.user.email, entity.file.name, entity.file.id | sort -events | nolimit
  merge:
    kind: aggregate
    key_cols:
    - actor.user.email
    - entity.file.name
    - entity.file.id
    sum_cols:
    - events
    approx_cols:
    - uniq_ips
    array_cols:
    - actions
  scope: subject
- id: imported-searches-slack-search-67
  title: Slack search 67
  notes: 'Source: slack | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_slack|slack}}' | filter action in:anycase('file_uploaded','file_shared','file_deleted','file_downloaded') | columns timestamp, actor.user.email, action, entity.file.name, entity.file.id, entity.file.filetype, entity.channel.name, context.ip_address, context.ua, details.url_private | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-slack-search-68
  title: Slack search 68
  notes: 'Source: slack | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_slack|slack}}' action='file_downloaded' context.app.name != 'Glean' context.ua != 'GleanSearchCrawlerBot' !(context.ua contains:anycase 'Scio Search') !(context.app.name contains:anycase 'Scio') | columns timestamp, actor.user.email, action, entity.file.name, entity.file.id, entity.file.filetype, entity.channel.name, context.ip_address, context.location.name, context.ua, details.url_private | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: opl-01-2
  title: Software Inventory by App/Tag
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_oplockdown|oplockdown}}' | group Endpoints=estimate_distinct(endpoint.name), Users=estimate_distinct(owner), FirstRecord.timestamp=min(timestamp), LastRecord.timestamp=max(timestamp), AppLastSeen.timestamp=max(lastseen) by app_name, tag | sort -Endpoints
  merge:
    kind: aggregate
    key_cols:
    - app_name
    - tag
    min_cols:
    - FirstRecord.timestamp
    max_cols:
    - LastRecord.timestamp
    - AppLastSeen.timestamp
    approx_cols:
    - Endpoints
    - Users
  scope: subject
- id: opl-05-2
  title: Software by User / Owner
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_oplockdown|oplockdown}}' | filter owner contains:anycase('{{username}}') | group Endpoints=estimate_distinct(endpoint.name), FirstRecord.timestamp=min(timestamp), LastRecord.timestamp=max(timestamp), AppLastSeen.timestamp=max(lastseen) by owner, endpoint.name, app_name, tag | sort -AppLastSeen.timestamp | nolimit
  merge:
    kind: aggregate
    key_cols:
    - owner
    - endpoint.name
    - app_name
    - tag
    min_cols:
    - FirstRecord.timestamp
    max_cols:
    - LastRecord.timestamp
    - AppLastSeen.timestamp
    approx_cols:
    - Endpoints
  scope: subject
- id: zia-dlp-37-2
  title: Source Code / Sensitive Docs Written to USB
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'File Creation' AND tgt.file.location == 'External Device' ) | filter( tgt.file.extension in:anycase('go','rs','py','js','ts','c','cpp','h','java','cs','kt','swift','sql','env','pem','key','p12','pfx','kdbx','csv','xlsx','xls','docx','pptx','pdf') ) | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), File=tgt.file.path, Ext=tgt.file.extension, Size=(tgt.file.size || 0), Proc=(src.process.name || src.process.image.path), Cmd=src.process.cmdline | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-12-2
  title: Source Code Repositories
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('github.com','githubusercontent.com','gitlab.com','bitbucket.org') | columns ts, event.user, event.deviceowner, event.devicehostname, host, event.url, event.appname, event.outbytes, event.action, event.rulelabel | sort -ts | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-09-2
  title: Suspicious File Types / Extensions
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let method=(event.requestmethod || event.method || '') | let url=(event.url || event.hostname || event.dns_req || '') | let fname=(event.filename || event.file_name || event.uploadedfilename || '') | let ftype=(event.filetype || event.contenttype || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let suspicious=(fname contains:anycase('.zip','.rar','.7z','.pst','.mbox','.sql','.db','.bak','.tar','.gz','.tgz','.csv','.xlsx','.docx','.pdf') OR ftype contains:anycase('zip','rar','7z','pst','octet-stream')) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | filter isUpload AND suspicious | columns ts, event.user, event.deviceowner, event.devicehostname, fname, ftype, outB, url, event.appname, event.action, event.rulelabel | sort -ts | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-08-2
  title: Top Upload Destinations
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let method=(event.requestmethod || event.method || '') | let host=(event.hostname || event.url || event.dns_req || '') | let url=(event.url || event.hostname || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | filter isUpload | group events=count(), total_out=sum(outB), users=estimate_distinct(event.user) by host, event.appname, event.urlcategory | sort -total_out | nolimit
  merge:
    kind: aggregate
    key_cols:
    - host
    - event.appname
    - event.urlcategory
    sum_cols:
    - events
    - total_out
    approx_cols:
    - users
  scope: subject
- id: zia-dlp-06-2
  title: Top Users by Outbound Upload Volume
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let method=(event.requestmethod || event.method || '') | let url=(event.url || event.hostname || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | filter isUpload | group total_out=sum(outB), events=count(), destinations=estimate_distinct(event.hostname || event.url || event.dns_req) by event.user, event.deviceowner, event.devicehostname | sort -total_out | nolimit
  merge:
    kind: aggregate
    key_cols:
    - event.user
    - event.deviceowner
    - event.devicehostname
    sum_cols:
    - total_out
    - events
    approx_cols:
    - destinations
  scope: subject
- id: zia-dlp-04-2
  title: Total Outbound Bytes (All ZIA)
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | group total_out=sum(event.outbytes || ZENTotalBytesTxClient || 0)
  merge:
    kind: append
  scope: subject
- id: zia-dlp-32-2
  title: Transfer Tools (scp / sftp / rsync / curl / wget)
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter( event.type == 'Process Creation' OR event.type == 'Process Start' OR dataSource.category == 'process' ) | let Proc=(src.process.name || tgt.process.name || process.name || '') | let Cmd=(src.process.cmdline || tgt.process.cmdline || process.cmdline || '') | filter( Proc contains:anycase('scp','sftp','rsync','curl','wget') OR Cmd contains:anycase(' scp ',' sftp ',' rsync ',' curl ',' wget ') ) | columns timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc, Cmd, Parent=(src.process.parent.name || ''), Path=(src.process.image.path || tgt.process.image.path || '') | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: endpoint-exfil-tooling-transfer-tools-scp-sftp-rsync-curl-wget
  title: Transfer Tools scp / sftp / rsync / curl / wget
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('Process Creation','Process Start','Process') | let Proc=(src.process.name || tgt.process.name || process.name || '') | let Cmd=(src.process.cmdline || tgt.process.cmdline || process.cmdline || '') | filter Proc contains:anycase('scp','sftp','rsync','curl','wget') OR Cmd contains:anycase(' scp ',' sftp ',' rsync ',' curl ',' wget ') | columns ts.timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc, Cmd, Parent=(src.process.parent.name || ''), Path=(src.process.image.path || tgt.process.image.path || '') | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-36-2
  title: USB Device Connections
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" cat='EndpointDeviceControlEvent' endpointDeviceControlClass='08h' | columns timestamp, Endpoint=sourceHostName, OS=sourceOsType, Device=endpointDeviceControlDeviceName, Action=endpointDeviceControlAction, User=(src.process.user || user.name) | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-02-2
  title: Unique Users with Upload-Like Activity
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let method=(event.requestmethod || event.method || '') | let url=(event.url || event.hostname || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | filter isUpload | group users=estimate_distinct(event.user)
  merge:
    kind: append
  scope: subject
- id: zia-dlp-17-2
  title: Unsanctioned / Shadow IT App Use
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' event.appname=* | group events=count(), users=estimate_distinct(event.user), total_out=sum(event.outbytes || 0) by event.appname, event.urlcategory | sort -total_out | nolimit
  merge:
    kind: aggregate
    key_cols:
    - event.appname
    - event.urlcategory
    sum_cols:
    - events
    - total_out
    approx_cols:
    - users
  scope: subject
- id: user-source-coverage
  title: User -> Source Coverage
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: any( actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, ipAddress, ip, session, event.client_id, event.app_name, event.api_name, id.applicationName, "Source\ User" ) contains '{{entity}}' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: user-timeline
  title: User -> Timeline
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: any( actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, username, detail.payload.Username, email, login, elogin, account, owner, operator, src.process.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, ipAddress, ip, session, event.client_id, event.app_name, event.api_name, id.applicationName, "Source\ User" ) contains '{{entity}}' | columns timestamp, serverHost, actor.email, actor.alternateId, user, user.email, username, owner, detail.payload.Username, USER_ID, ipAddress, event.name, event.type | sort timestamp | nolimit0
  merge:
    kind: append
  scope: subject
- id: lkp-01-2
  title: User / Device Identity Lookup
  notes: 'Source: all | Imported/generated for SOC investigation library.'
  pq: '| union (| dataset ''config://datatables/CostCenters'' | columns email, Hire_Date), (| dataset ''config://datatables/Accounts_Github'' | columns email, github.name=username | let glean=''https://app.glean.com/directory/people/profile?person=''+email), (| dataset ''config://datatables/Accounts_Jamf'' | columns ''jamf.comp''=Computer_Name,''jamf.name''=Username,email), (| dataset ''config://datatables/Accounts_Google'' | columns google.name,email,google.email,google.aliases), (| dataset ''config://datatables/Accounts_intune'' | columns email,''intune.comp''=displayName), (| dataset ''config://datatables/jumpcloud_user_summary'' | columns email,''jumpcloud.comp''=hostname,jumpcloud.user=username) | filter email == ''{{entity}}'' | columns email, Hire_Date, github.name, jamf.comp, jamf.name, google.name, google.email, google.aliases, intune.comp, jumpcloud.comp, jumpcloud.user | sort +email'
  merge:
    kind: append
  scope: subject
- id: zia-dlp-24-2
  title: User Risk Rollup
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let hour=hourofday(ts) | let method=(event.requestmethod || event.method || '') | let host=(event.hostname || event.url || event.dns_req || '') | let url=(event.url || event.hostname || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | let isUpload=(method in ('POST','PUT') OR url contains:anycase('upload','put','attachment','share','files','api') OR outB > 5000000) | let isAfterHours=(hour < 7 OR hour > 19) | let isHighRisk=(host contains:anycase('wetransfer','mega','mediafire','pastebin','gist.github','dropbox','drive.google','icloud','github','gitlab','bitbucket')) | filter isUpload | group events=count(), total_out=sum(outB), destinations=estimate_distinct(host), after_hours=count(if(isAfterHours,1,null)), high_risk=count(if(isHighRisk,1,null)) by event.user, event.deviceowner, event.devicehostname | sort -total_out | nolimit
  merge:
    kind: aggregate
    key_cols:
    - event.user
    - event.deviceowner
    - event.devicehostname
    sum_cols:
    - events
    - total_out
    - after_hours
    - high_risk
    approx_cols:
    - destinations
  scope: subject
- id: potential-block-candidates-users-requiring-business-justification-revi
  title: Users Requiring Business Justification Review
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let host=(event.hostname || event.url || event.dns_req || '') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter host contains:anycase('icloud','wetransfer','swisstransfer','mega','telegram','signal','discord','archive.org','codeium','claude','anthropic','glean','perplexity','openai','chatgpt') | group upload_bytes=sum(outB), apps=estimate_distinct(event.appname), destinations=estimate_distinct(host), events=count() by event.user, event.devicehostname | filter upload_bytes > 50000000 OR destinations > 5 | sort -upload_bytes | nolimit
  merge:
    kind: aggregate
    key_cols:
    - event.user
    - event.devicehostname | filter upload_bytes > 50000000 OR destinations > 5
    sum_cols:
    - upload_bytes
    - events
    approx_cols:
    - apps
    - destinations
  scope: subject
- id: zia-bp-01-2
  title: Users with ZIA Disable Events
  notes: 'Source: zia_disable_reason | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia_disable_reason|zia_disable_reason}}' | group events=count(), reasons=array_agg(DisableReason,20) by User | sort -events
  merge:
    kind: aggregate
    key_cols:
    - User
    sum_cols:
    - events
    array_cols:
    - reasons
  scope: subject
- id: zia-dlp-19-2
  title: VPN / Proxy / Anonymizer Usage
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('torproject.org','protonvpn','nordvpn','expressvpn','surfshark','tunnelbear','hide.me','openvpn') OR event.urlcategory contains:anycase('anonymizer','proxy avoidance') | columns ts, event.user, event.deviceowner, event.devicehostname, host, event.url, event.action, event.rulelabel, event.destcountry | sort -ts | nolimit
  merge:
    kind: append
  scope: subject
- id: remote-access-vpn-p2p-vpn-proxy-anonymizer-zia-activity
  title: VPN / Proxy / Anonymizer ZIA Activity
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('torproject.org','protonvpn','nordvpn','expressvpn','surfshark','tunnelbear','hide.me','openvpn','tailscale','wireguard') OR event.urlcategory contains:anycase('anonymizer','proxy avoidance') | columns ts.timestamp=ts, event.user, event.devicehostname, host, event.url, event.action, event.rulelabel, event.destcountry | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-dlp-18-2
  title: Webmail Activity
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('mail.google.com','gmail.com','outlook.live.com','mail.yahoo.com','proton.me','protonmail.com') | columns ts, event.user, event.deviceowner, event.devicehostname, host, event.url, event.outbytes, event.action, event.rulelabel | sort -ts | nolimit
  merge:
    kind: append
  scope: subject
- id: corr-03
  title: Workspace Sensitive Doc + ZIA Upload
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" Use ZIA-DLP-43 Sensitive Activity + ZIA Egress; then pivot user_name and timestamp to raw Workspace and ZIA panels.
  merge:
    kind: append
  scope: subject
- id: imported-searches-zia-egress-search-10
  title: ZIA / Egress search 10
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.appname='iCloud' OR event.hostname contains:anycase('icloud') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter event.requestmethod in ('POST','PUT') OR outB > 5000000 | columns timestamp,event.user,event.devicehostname,event.hostname,event.url,event.requestmethod,event.action,outB | sort -outB|nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-zia-egress-search-11
  title: ZIA / Egress search 11
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.appname='iCloud' OR event.hostname contains:anycase('icloud') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | group upload_bytes=sum(outB), events=count() by event.user,event.devicehostname,event.deviceowner | sort -upload_bytes
  merge:
    kind: aggregate
    key_cols:
    - event.user
    - event.devicehostname
    - event.deviceowner
    sum_cols:
    - upload_bytes
    - events
  scope: subject
- id: imported-searches-zia-egress-search-12
  title: ZIA / Egress search 12
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.hostname contains:anycase('pastebin','gist.github','rentry','archive.org','ghostbin','hastebin') | columns timestamp,event.user,event.hostname,event.url,event.appname,event.action,event.outbytes|nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-zia-egress-search-13
  title: ZIA / Egress search 13
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.hostname contains:anycase('gmail','outlook.live','hotmail','yahoo','protonmail') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter outB > 1000000 | columns timestamp,event.user,event.hostname,event.url,event.appname,event.action,outB | sort -outB|nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-zia-egress-search-14
  title: ZIA / Egress search 14
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.hostname contains:anycase('icloud','wetransfer','swisstransfer','mega','dropbox','box','telegram','discord','signal','whatsapp','archive.org','mediafire') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | group upload_bytes=sum(outB), events=count() by event.user,event.hostname,event.appname,event.action | sort -upload_bytes
  merge:
    kind: aggregate
    key_cols:
    - event.user
    - event.hostname
    - event.appname
    - event.action
    sum_cols:
    - upload_bytes
    - events
  scope: subject
- id: imported-searches-zia-egress-search-15
  title: ZIA / Egress search 15
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' event.app_status='Unsanctioned' | filter event.urlcategory contains:anycase('AI','ML') OR event.appname contains:anycase('AI','Claude','ChatGPT','Gemini','Perplexity','Character','Andi') | group events=count(), users=estimate_distinct(event.user) by event.appname,event.hostname,event.action | sort -events
  merge:
    kind: aggregate
    key_cols:
    - event.appname
    - event.hostname
    - event.action
    sum_cols:
    - events
    approx_cols:
    - users
  scope: subject
- id: imported-searches-zia-egress-search-16
  title: ZIA / Egress search 16
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.hostname contains:anycase('claude.ai','anthropic.com','codeium.com','windsurf.ai','glean.com','openai.com','chatgpt.com','gemini.google.com','perplexity.ai') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | group upload_bytes=sum(outB), users=estimate_distinct(event.user), events=count() by event.hostname,event.appname | sort -upload_bytes
  merge:
    kind: aggregate
    key_cols:
    - event.hostname
    - event.appname
    sum_cols:
    - upload_bytes
    - events
    approx_cols:
    - users
  scope: subject
- id: imported-searches-zia-egress-search-17
  title: ZIA / Egress search 17
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.appname contains:anycase('Claude','Anthropic','Codeium','Windsurf','Glean','OpenAI','ChatGPT','Gemini','Copilot','Perplexity') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | group upload_bytes=sum(outB), events=count() by event.user,event.appname | sort -upload_bytes
  merge:
    kind: aggregate
    key_cols:
    - event.user
    - event.appname
    sum_cols:
    - upload_bytes
    - events
  scope: subject
- id: imported-searches-zia-egress-search-18
  title: ZIA / Egress search 18
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter outB > 10000000 | group upload_bytes=sum(outB), events=count() by event.user,event.hostname,event.appname | sort -upload_bytes
  merge:
    kind: aggregate
    key_cols:
    - event.user
    - event.hostname
    - event.appname
    sum_cols:
    - upload_bytes
    - events
  scope: subject
- id: imported-searches-zia-egress-search-19
  title: ZIA / Egress search 19
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter outB > 50000000 | columns timestamp,event.user,event.devicehostname,event.hostname,event.url,event.appname,event.action,outB | sort -outB|nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-zia-egress-search-20
  title: ZIA / Egress search 20
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' sourcetype='zscalernss-web' | group count=count() by timestamp=timebucket('1h'), event.action | transpose event.action
  merge:
    kind: aggregate
    key_cols:
    - timestamp
    - event.action
    sum_cols:
    - count
  scope: subject
- id: imported-searches-zia-egress-search-21
  title: ZIA / Egress search 21
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | group count=count() by timestamp=timebucket('1h'), sourcetype | transpose sourcetype
  merge:
    kind: aggregate
    key_cols:
    - timestamp
    - sourcetype
    sum_cols:
    - count
  scope: subject
- id: imported-searches-zia-egress-search-62
  title: ZIA / Egress search 62
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.url contains:anycase('upload') OR event.action contains:anycase('upload') OR event.reqaction contains:anycase('upload') OR event.resaction contains:anycase('upload') OR event.requestmethod in:anycase('POST','PUT') | columns timestamp, event.user, event.ClientIP, event.requestmethod, event.action, event.appname, event.urlcategory, event.url, event.outbytes, event.inbytes | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: imported-searches-zia-egress-search-64
  title: ZIA / Egress search 64
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' !(event.appname = 'Glean') !(event.appname = 'Scio Search') !(event.url contains:anycase 'glean') !(event.url contains:anycase 'scio') | filter event.url contains:anycase('drive.google','docs.google','dropbox','onedrive','box.com','wetransfer','mega','icloud','pastebin','github','s3.amazonaws') OR event.appname contains:anycase('Google Drive','Dropbox','OneDrive','Box','iCloud','MEGA','WeTransfer','GitHub') | columns timestamp, event.user, event.action, event.ClientIP, event.appname, event.urlcategory, event.url, event.refererURL, event.outbytes, event.inbytes, event.useragent | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-large-uploads
  title: ZIA Large Uploads
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter outB > 50000000 | columns timestamp,event.user,event.devicehostname,event.hostname,event.url,event.appname,event.action,event.requestmethod,outB,event.ClientIP,event.useragent | sort -outB | nolimit
  merge:
    kind: append
  scope: subject
- id: cov-04-2-2
  title: ZIA Web Proxy Coverage
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: serverHost='{{src_zia|zia}}' event.user='{{entity}}' | let ts=(timestamp || event.time || sca:ingestTime) | filter ts = * | group FirstSeen.timestamp=min(ts), LastSeen.timestamp=max(ts), EventCount=count(), ClientIPs=array_agg_distinct(event.ClientIP, 10), Apps=array_agg_distinct(event.appname, 10) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
    array_cols:
    - ClientIPs
    - Apps
  scope: subject
- id: zia-disable-bypass
  title: ZIA disable/bypass
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" Find ZIA disable events
  merge:
    kind: append
  scope: subject
- id: zia-large-upload
  title: ZIA large upload
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" Find large outbound web transfers
  merge:
    kind: append
  scope: subject
- id: cov-03-2-2
  title: ZPA / Zscaler Session Coverage
  notes: 'Source: zscaler | Imported/generated for SOC investigation library.'
  pq: serverHost='{{src_zscaler|zscaler}}' | filter actor.email contains:anycase('{{entity}}') OR actor.email contains:anycase('{{entity}}') OR Username contains:anycase('{{entity}}') OR Username contains:anycase('{{entity}}') OR event.users contains:anycase('{{entity}}') OR event.users contains:anycase('{{entity}}') | let ts=(TimestampConnectionStart || timestamp || sca:ingestTime) | filter ts = * | group FirstSeen.timestamp=min(ts), LastSeen.timestamp=max(ts), EventCount=count(), Countries=array_agg_distinct(ClientCountryCode, 10), PublicIPs=array_agg_distinct(ClientPublicIP, 10) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
    array_cols:
    - Countries
    - PublicIPs
  scope: subject
- id: google-workspace-classification-label-changes
  title: Classification Label Changes
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_google_workspace|google_workspace}}' | filter event.name == 'label_field_changed' OR classificationLabel = * | columns ts.timestamp=(id.time || timestamp), actor.email, event.owner, event.doc_title, event.doc_id, classificationLabel, event.label_title, event.field, event.new_value, event.new_field_value, ipAddress, actor.applicationInfo.applicationName | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: google-workspace-drive-downloads-external-sharing
  title: Drive Downloads / External Sharing
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_google_workspace|google_workspace}}' | filter event.name in:anycase('download','sync_item_content','access_item_content','change_document_visibility','change_document_access_scope','change_acl_editors','change_user_access','print','copy') | columns ts.timestamp=(id.time || timestamp), actor.email, event.name, event.doc_title, event.doc_id, event.doc_type, event.owner, event.visibility, event.old_visibility, classificationLabel, ipAddress, networkInfo.regionCode, actor.applicationInfo.applicationName | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: google-workspace-drive-classification-label-changes
  title: Classification Label Changes
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost == 'google_workspace' | filter actor.applicationInfo.applicationName != 'SCIO Search' | filter event.name == 'label_field_changed' AND event.field == 'Data Sensitivity Levels' | columns id.time, actor.email, event.owner, event.doc_title, event.doc_id, event.doc_type, event.label_title, event.field, event.new_value, event.new_field_value, ipAddress, networkInfo.regionCode, networkInfo.subdivisionCode, actor.applicationInfo.applicationName | sort -id.time | nolimit
  merge:
    kind: append
  scope: subject
- id: google-workspace-drive-drive-downloads-external-sharing
  title: Drive Downloads / External Sharing
  notes: 'Source: google_workspace | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost == 'google_workspace' | filter event.name in:anycase('download','sync_item_content','access_item_content','change_document_visibility','change_document_access_scope','shared_drive_file_downloaded','copy','print') OR event.visibility in:anycase('shared_externally','public') | columns timestamp=(id.time || timestamp), actor.email, event.name, event.doc_title, event.doc_id, event.doc_type, event.owner, event.visibility, event.old_visibility, classificationLabel, ipAddress, actor.applicationInfo.applicationName | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: oplockdown-software-risk-high-risk-software-by-user-endpoint
  title: High-Risk Software by User / Endpoint
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_oplockdown|oplockdown}}' | filter tag in ('VPN','RemoteManagement','Filesharing','Peer-To-Peer','AI-IDE','AI-MISUSE','Games') | columns timestamp, owner, endpoint.name, app_name, tag, severity, LastSeen.timestamp=lastseen | sort -LastSeen.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: oplockdown-software-risk-recently-observed-high-risk-software
  title: Recently Observed High-Risk Software
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_oplockdown|oplockdown}}' | filter tag in ('VPN','RemoteManagement','Filesharing','Peer-To-Peer','AI-IDE','AI-MISUSE','Games') | group FirstRecord.timestamp=min(timestamp), LastRecord.timestamp=max(timestamp), AppLastSeen.timestamp=max(lastseen), Owners=array_agg_distinct(owner,20) by app_name, tag, endpoint.name | sort -AppLastSeen.timestamp | nolimit
  merge:
    kind: aggregate
    key_cols:
    - app_name
    - tag
    - endpoint.name
    min_cols:
    - FirstRecord.timestamp
    max_cols:
    - LastRecord.timestamp
    - AppLastSeen.timestamp
    array_cols:
    - Owners
  scope: subject
- id: oplockdown-software-risk-software-inventory-by-app-tag
  title: Software Inventory by App / Tag
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_oplockdown|oplockdown}}' | group Endpoints=estimate_distinct(endpoint.name), Users=estimate_distinct(owner), FirstRecord.timestamp=min(timestamp), LastRecord.timestamp=max(timestamp), AppLastSeen.timestamp=max(lastseen) by app_name, tag | sort -Endpoints | nolimit
  merge:
    kind: aggregate
    key_cols:
    - app_name
    - tag
    min_cols:
    - FirstRecord.timestamp
    max_cols:
    - LastRecord.timestamp
    - AppLastSeen.timestamp
    approx_cols:
    - Endpoints
    - Users
  scope: subject
- id: oplockdown-software-risk-unowned-orphaned-software
  title: Unowned / Orphaned Software
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_oplockdown|oplockdown}}' | filter owner == '-' OR !owner OR owner == '' | columns timestamp, endpoint.name, app_name, tag, severity, LastSeen.timestamp=lastseen | sort -LastSeen.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: prompt-security-ai-ai-apps-installed-zia-usage-candidate-list
  title: AI Apps Installed + ZIA Usage Candidate List
  notes: 'Source: oplockdown | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union ( serverHost='{{src_oplockdown|oplockdown}}' | filter tag in ('AI-IDE','AI-MISUSE') OR app_name contains:anycase('Cursor','Perplexity','Claude','ChatGPT','Windsurf','Copilot','Gemini') | columns ts.timestamp=timestamp, User=owner, endpoint.name, signal='installed_ai_app', app=app_name, detail=tag, bytes=0 ), ( serverHost='{{src_zia|zia}}' | filter event.appname contains:anycase('Claude','Anthropic','Codeium','Windsurf','Glean','OpenAI','ChatGPT','Gemini','Copilot','Perplexity','Cursor') OR event.hostname contains:anycase('claude.ai','anthropic.com','openai.com','chatgpt.com','gemini.google.com','perplexity.ai','cursor.com','cursor.sh') | columns ts.timestamp=(event.datetime || timestamp), User=event.user, endpoint.name=event.devicehostname, signal='zia_ai_usage', app=event.appname, detail=event.hostname, bytes=(event.outbytes || ZENTotalBytesTxClient || 0) ) | sort User, -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: prompt-security-ai-prompt-violations-drilldown
  title: Prompt Violations Drilldown
  notes: 'Source: prompt-security | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_prompt_security|prompt-security}}' | filter log.isValid == true | let ts=(log.timestamp || log.event_time || event_time || timestamp || ReceiveTime) | filter log.violations = * OR log.violationFindingsDetails.Secrets = * OR log.violationFindingsDetails.Sensitive_Data = * OR log.violationFindingsDetails.Harmful_Content_Moderator = * | columns ts.timestamp=ts, log.user, log.userGroups, log.application.name, log.sensor.pageDomain, log.sensor.pageUrl, log.ipAddress, log.country, log.policyName, log.violations, log.violationFindingsDetails.Secrets, log.violationFindingsDetails.Sensitive_Data, log.violationFindingsDetails.Harmful_Content_Moderator, log.conversationId, log.promptResponseId, severity | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: raw-quick-pivots-raw-edr-user-endpoint-timeline
  title: Raw EDR User / Endpoint Timeline
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category in ('file','process','dns','ip') | columns timestamp=(event.time || timestamp), event.category, event.type, endpoint.name, agent.uuid, User=(src.process.user || user.name), Proc=(src.process.name || process.name), Cmd=(src.process.cmdline || process.cmdline), File=tgt.file.path, DNS=event.dns.request, DstIP=dst.ip.address, Storyline=src.process.storyline.id | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: raw-quick-pivots-raw-zia-activity
  title: Raw ZIA Activity
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | columns ts, event.user, event.deviceowner, event.devicehostname, event.requestmethod, event.method, event.url, event.hostname, event.appname, event.urlcategory, event.urlsupercategory, event.destcountry, event.action, event.rulelabel, event.inbytes, event.outbytes, event.threatcat, event.threatname, event.threat_score, event.threat_severity | sort -ts | nolimit
  merge:
    kind: append
  scope: subject
- id: raw-quick-pivots-user-source-coverage
  title: User Source Coverage
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, email, login, elogin, account, owner, operator, src.process.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, ipAddress, ip, session, event.client_id, event.app_name, event.api_name, id.applicationName, detail.payload.Username, detail.payload.UserId, username, 'Source\ User') contains '*' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Source=serverHost | sort -EventCount | nolimit
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: recovered-airdrop-sharing-daemon-file-writes
  title: AirDrop / Sharing Daemon File Writes
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter event.type='File Creation' | filter src.process.name contains:anycase('sharingd') OR src.process.image.path contains:anycase('sharingd') | columns event.time, endpoint.name, src.process.user, src.process.name, tgt.file.path, tgt.file.size, src.process.storyline.id | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-apple-icloud-dns-activity
  title: Apple / iCloud DNS Activity
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='dns' | filter event.dns.request contains:anycase('icloud') OR event.dns.request contains:anycase('icloud-content') OR event.dns.request contains:anycase('apple') OR event.dns.request contains:anycase('mzstatic') OR event.dns.request contains:anycase('cdn-apple') | columns event.time, endpoint.name, src.process.user, src.process.name, event.dns.request, event.dns.response, src.process.storyline.id | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-apple-sharing-airdrop-process-activity
  title: Apple Sharing / AirDrop Process Activity
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='process' | filter src.process.name contains:anycase('sharingd') OR src.process.cmdline contains:anycase('AirDrop') OR src.process.cmdline contains:anycase('sharingd') | columns event.time, endpoint.name, src.process.user, src.process.name, src.process.image.path, src.process.cmdline, src.process.parent.name, src.process.storyline.id | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-broad-icloud-user-endpoint-timeline
  title: Broad iCloud User / Endpoint Timeline
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter * contains:anycase('icloud') OR * contains:anycase('CloudDocs') OR * contains:anycase('Mobile Documents') OR * contains:anycase('com~apple~CloudDocs') | columns event.time, serverHost, endpoint.name, agent.uuid, event.category, event.type, src.process.user, src.process.name, src.process.cmdline, tgt.file.path, dst.ip.address, event.dns.request, url.address, src.process.storyline.id | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-downloads-to-icloud-path
  title: Downloads to iCloud Path
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('/Downloads/') | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('CloudDocs') | columns event.time, endpoint.name, src.process.user, src.process.name, event.type, tgt.file.path, tgt.file.size | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-endpoint-icloud-summary
  title: Endpoint iCloud Summary
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter * contains:anycase('icloud') OR * contains:anycase('CloudDocs') OR * contains:anycase('Mobile Documents') OR * contains:anycase('com~apple~CloudDocs') | group events=count(), users=array_agg_distinct(src.process.user,10), processes=array_agg_distinct(src.process.name,10), file_paths=estimate_distinct(tgt.file.path) by endpoint.name, agent.uuid | sort -events | nolimit
  merge:
    kind: aggregate
    key_cols:
    - endpoint.name
    - agent.uuid
    sum_cols:
    - events
    approx_cols:
    - file_paths
    array_cols:
    - users
    - processes
  scope: subject
- id: recovered-large-files-written-to-icloud-100mb
  title: Large Files Written to iCloud >100MB
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | filter tgt.file.size > 104857600 | columns event.time, endpoint.name, src.process.user, src.process.name, event.type, tgt.file.path, SizeMB=(tgt.file.size/1024/1024), tgt.file.sha1, src.process.storyline.id | sort -SizeMB | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-large-icloud-files-100mb
  title: Large iCloud Files >100MB
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | filter tgt.file.size > 104857600 | group large_files=count()
  merge:
    kind: append
  scope: subject
- id: recovered-raw-icloud-file-timeline
  title: Raw iCloud File Timeline
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | columns event.time, endpoint.name, agent.uuid, src.process.user, src.process.name, event.type, tgt.file.path, tgt.file.size, tgt.file.sha1, src.process.storyline.id | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-top-users-by-icloud-file-activity
  title: Top Users by iCloud File Activity
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | group events=count(), endpoints=estimate_distinct(endpoint.name) by src.process.user | sort -events | limit 20
  merge:
    kind: aggregate
    key_cols:
    - src.process.user
    sum_cols:
    - events
    approx_cols:
    - endpoints
  scope: subject
- id: recovered-top-icloud-processes
  title: Top iCloud Processes
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='process' | filter src.process.name contains:anycase('bird') OR src.process.name contains:anycase('cloudd') OR src.process.name contains:anycase('CloudDocs') OR src.process.name contains:anycase('cloudphotod') OR src.process.name contains:anycase('icloud') OR src.process.cmdline contains:anycase('icloud') OR src.process.cmdline contains:anycase('CloudDocs') | group events=count(), users=estimate_distinct(src.process.user), endpoints=estimate_distinct(endpoint.name) by src.process.name | sort -events | limit 20
  merge:
    kind: aggregate
    key_cols:
    - src.process.name
    sum_cols:
    - events
    approx_cols:
    - users
    - endpoints
  scope: subject
- id: recovered-top-icloud-processes-2
  title: Top iCloud Processes
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='process' | filter src.process.name in ('bird','cloudd','cloudphotod','nsurlsessiond','sharingd') | group events=count() by src.process.name | sort -events
  merge:
    kind: aggregate
    key_cols:
    - src.process.name
    sum_cols:
    - events
  scope: subject
- id: recovered-unique-endpoints
  title: Unique Endpoints
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | group endpoints=estimate_distinct(endpoint.name)
  merge:
    kind: append
  scope: subject
- id: recovered-unique-users
  title: Unique Users
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | group users=estimate_distinct(src.process.user)
  merge:
    kind: append
  scope: subject
- id: recovered-user-icloud-summary
  title: User iCloud Summary
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | filter * contains:anycase('icloud') OR * contains:anycase('CloudDocs') OR * contains:anycase('Mobile Documents') OR * contains:anycase('com~apple~CloudDocs') | group events=count(), endpoints=array_agg_distinct(endpoint.name,10), processes=array_agg_distinct(src.process.name,10), file_paths=estimate_distinct(tgt.file.path) by src.process.user | sort -events | nolimit
  merge:
    kind: aggregate
    key_cols:
    - src.process.user
    sum_cols:
    - events
    approx_cols:
    - file_paths
    array_cols:
    - endpoints
    - processes
  scope: subject
- id: recovered-icloud-apple-ip-connect-activity
  title: iCloud / Apple IP Connect Activity
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='ip' | filter src.process.name contains:anycase('bird') OR src.process.name contains:anycase('cloudd') OR src.process.name contains:anycase('CloudDocs') OR src.process.name contains:anycase('cloudphotod') OR src.process.name contains:anycase('icloud') | columns event.time, endpoint.name, src.process.user, src.process.name, src.ip.address, dst.ip.address, dst.port.number, dst.geo.country, dst.as.organization, event.network.direction, event.network.connectionStatus, src.process.storyline.id | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-icloud-activity-over-time
  title: iCloud Activity Over Time
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | group events=count() by timestamp=timebucket(event.time,'1h'), event.type | transpose event.type on timestamp
  merge:
    kind: aggregate
    key_cols:
    - timestamp
    - '''1h'')'
    - event.type
    sum_cols:
    - events
  scope: subject
- id: recovered-icloud-activity-by-user-endpoint
  title: iCloud Activity by User / Endpoint
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category in ('file','process','dns','ip') | filter src.process.name in ('bird','cloudd','cloudphotod','nsurlsessiond','sharingd') OR tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('CloudDocs') OR event.dns.request contains:anycase('icloud') | group events=count(), processes=array_agg_distinct(src.process.name), event_types=array_agg_distinct(event.type) by endpoint.name, src.process.user | sort -events | nolimit
  merge:
    kind: aggregate
    key_cols:
    - endpoint.name
    - src.process.user
    sum_cols:
    - events
    array_cols:
    - processes
    - event_types
  scope: subject
- id: recovered-icloud-dns-activity
  title: iCloud DNS Activity
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type='DNS Resolved' | filter event.dns.request contains:anycase('icloud') OR event.dns.request contains:anycase('icloud-content') OR event.dns.request contains:anycase('apple-dns') OR event.dns.request contains:anycase('ckdatabase') | columns event.time, endpoint.name, src.process.user, src.process.name, event.dns.request, event.dns.response | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-icloud-drive-upload-sync-candidates
  title: iCloud Drive Upload / Sync Candidates
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter src.process.name in ('bird','cloudd','cloudphotod','nsurlsessiond') | filter event.type in ('File Creation','File Modification','File Rename') | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('CloudDocs') | columns event.time, endpoint.name, src.process.user, src.process.name, event.type, tgt.file.path, tgt.file.size | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-icloud-events-by-type
  title: iCloud Events by Type
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | group events=count() by event.type | sort -events
  merge:
    kind: aggregate
    key_cols:
    - event.type
    sum_cols:
    - events
  scope: subject
- id: recovered-icloud-file-activity
  title: iCloud File Activity
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('CloudDocs') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | columns event.time, endpoint.name, src.process.user, src.process.name, event.type, tgt.file.path, tgt.file.size | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-icloud-file-creates-modifies-renames
  title: iCloud File Creates / Modifies / Renames
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter event.type in ('File Creation','File Modification','File Rename') | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') OR tgt.file.oldPath contains:anycase('Mobile Documents') OR tgt.file.oldPath contains:anycase('iCloud Drive') | columns event.time, endpoint.name, src.process.user, src.process.name, event.type, old_path=tgt.file.oldPath, new_path=tgt.file.path, tgt.file.size | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-icloud-file-deletions
  title: iCloud File Deletions
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter event.type='File Deletion' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | columns event.time, endpoint.name, src.process.user, src.process.name, tgt.file.path, tgt.file.size, src.process.storyline.id | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-icloud-file-events
  title: iCloud File Events
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | group events=count()
  merge:
    kind: append
  scope: subject
- id: recovered-icloud-file-volume-by-user-endpoint
  title: iCloud File Volume by User / Endpoint
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') | group events=count(), files=estimate_distinct(tgt.file.path), total_mb=sum(tgt.file.size)/1024/1024 by src.process.user, endpoint.name | sort -total_mb | nolimit
  merge:
    kind: aggregate
    key_cols:
    - src.process.user
    - endpoint.name
    sum_cols:
    - events
    - total_mb
    approx_cols:
    - files
  scope: subject
- id: recovered-icloud-network-connections
  title: iCloud Network Connections
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type='IP Connect' | filter src.process.name in ('bird','cloudd','cloudphotod','nsurlsessiond','sharingd') | columns event.time, endpoint.name, src.process.user, src.process.name, dst.ip.address, dst.port.number, dst.geo.country, dst.as.organization, event.network.direction, event.network.connectionStatus | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: recovered-icloud-process-activity-over-time
  title: iCloud Process Activity Over Time
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='process' | filter src.process.name in ('bird','cloudd','cloudphotod','nsurlsessiond','sharingd') | group events=count() by timestamp=timebucket(event.time,'1h'), src.process.name | transpose src.process.name on timestamp
  merge:
    kind: aggregate
    key_cols:
    - timestamp
    - '''1h'')'
    - src.process.name
    sum_cols:
    - events
  scope: subject
- id: recovered-icloud-related-process-activity
  title: iCloud-Related Process Activity
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='process' | filter src.process.name contains:anycase('bird') OR src.process.name contains:anycase('cloudd') OR src.process.name contains:anycase('CloudDocs') OR src.process.name contains:anycase('cloudphotod') OR src.process.name contains:anycase('icloud') OR src.process.cmdline contains:anycase('icloud') OR src.process.cmdline contains:anycase('CloudDocs') | columns event.time, endpoint.name, agent.uuid, src.process.user, src.process.name, src.process.image.path, src.process.cmdline, src.process.parent.name, src.process.storyline.id | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: salesforce-salesforce-audit-changes
  title: Salesforce Audit Changes
  notes: 'Source: salesforce_audit | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_audit|salesforce_audit}}' | columns ts.timestamp=(CreatedDate || SystemModstamp || timestamp), username, CreatedById, Action, Section, Display, DelegateUser, ResponsibleNamespacePrefix, session, serverIP | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: salesforce-salesforce-audit-changes-2
  title: Salesforce Audit Changes
  notes: 'Source: salesforce_audit | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_audit|salesforce_audit}}' | columns timestamp=(CreatedDate || SystemModstamp || timestamp), username, CreatedById, Action, Section, Display, DelegateUser, ResponsibleNamespacePrefix, session | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: salesforce-salesforce-event-monitoring-exports-api
  title: Salesforce Event Monitoring Exports / API
  notes: 'Source: salesforce_event | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_event|salesforce_event}}' | filter EVENT_TYPE in:anycase('ReportExport','Report','API','RestApi','BulkApi','ContentTransfer','ContentDistribution') OR URI contains:anycase('export','report') | columns ts.timestamp=(TIMESTAMP_DERIVED || TIMESTAMP || timestamp), USER_ID, username, EVENT_TYPE, CLIENT_IP, USER_AGENT, URI, ENTITY_NAME, NUMBER_OF_RECORDS, ROW_COUNT, REQUEST_STATUS, LOGIN_KEY, SESSION_KEY | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: salesforce-salesforce-event-monitoring-exports-api-2
  title: Salesforce Event Monitoring Exports / API
  notes: 'Source: salesforce_event | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_event|salesforce_event}}' | filter EVENT_TYPE contains:anycase('ReportExport','API','BulkApi','RestApi','ContentTransfer','ContentDistribution') OR USER_ID = * | columns timestamp=(TIMESTAMP_DERIVED || TIMESTAMP || timestamp), USER_ID, USER_ID_DERIVED, CLIENT_IP, APP_NAME, EVENT_TYPE, ENTITY_NAME, REQUEST_STATUS, RESPONSE_SIZE, ROW_COUNT=NUMBER_OF_RECORDS, URI, USER_AGENT, SESSION_KEY, LOGIN_KEY | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: salesforce-salesforce-realtime-report-file-activity
  title: Salesforce Realtime Report / File Activity
  notes: 'Source: salesforce-realtime | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_realtime|salesforce-realtime}}' | filter detail.payload.Operation contains:anycase('Report','Export','Download','File') OR detail.payload.FileAction = * OR detail.payload.ReportId = * | columns ts.timestamp=(detail.payload.EventDate || time || timestamp), detail.payload.Username, detail.payload.UserId, detail.payload.Operation, detail.payload.Status, detail.payload.ReportId, detail.payload.Name, detail.payload.RowsProcessed, detail.payload.RowsReturned, detail.payload.ContentSize, detail.payload.SourceIp, detail.payload.UserAgent | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: salesforce-salesforce-realtime-report-file-activity-2
  title: Salesforce Realtime Report / File Activity
  notes: 'Source: salesforce-realtime | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_salesforce_realtime|salesforce-realtime}}' | filter detail.payload.Username = * | filter detail.payload.ReportId = * OR detail.payload.RowsReturned > 0 OR detail.payload.RowsProcessed > 0 OR detail.payload.FileAction = * OR detail.payload.Operation contains:anycase('Export','Download') | columns timestamp=(detail.payload.EventDate || detail.payload.CreatedDate || time || timestamp), User=detail.payload.Username, UserId=detail.payload.UserId, SourceIp=detail.payload.SourceIp, Operation=detail.payload.Operation, App=detail.payload.AppName, ReportId=detail.payload.ReportId, RowsReturned=detail.payload.RowsReturned, RowsProcessed=detail.payload.RowsProcessed, ContentSize=detail.payload.ContentSize, FileAction=detail.payload.FileAction, Query=detail.payload.Query, SessionKey=detail.payload.SessionKey | sort -timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: usb-endpoint-staging-large-archive-creation-50mb
  title: Large Archive Creation >50MB
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('File Creation','File Modification') | filter tgt.file.extension in:anycase('zip','rar','7z','tar','gz','tgz','pst','mbox') | let Size=(tgt.file.size || file.size || 0) | filter Size > 50000000 | columns ts.timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), File=tgt.file.path, Ext=tgt.file.extension, SizeMB=(Size/1024/1024), Proc=(src.process.name || process.name || src.process.image.path), Cmd=src.process.cmdline | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: usb-endpoint-staging-usb-external-device-writes-refined
  title: USB / External Device Writes - Refined
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.type in ('File Creation','File Modification') | filter tgt.file.location == 'External Device' OR tgt.file.path contains:anycase('/Volumes/') | filter !(tgt.file.path contains:anycase('/var/lib/kubelet/')) AND !(tgt.file.path contains:anycase('/pods/')) AND !(tgt.file.path contains:anycase('/projected/')) AND !(src.process.name contains:anycase('kubelet')) | columns ts.timestamp=(event.time || timestamp), endpoint.name, agent.uuid, User=(src.process.user || user.name), File=tgt.file.path, Ext=tgt.file.extension, Size=tgt.file.size, Proc=(src.process.name || process.name || src.process.image.path), Cmd=src.process.cmdline | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-egress-dlp-ai-application-usage-upload-volume
  title: AI Application Usage / Upload Volume
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.appname contains:anycase('Claude','Anthropic','Codeium','Windsurf','Glean','OpenAI','ChatGPT','Gemini','Copilot','Perplexity','Cursor') OR event.hostname contains:anycase('claude.ai','anthropic.com','openai.com','chatgpt.com','gemini.google.com','perplexity.ai','cursor.com','cursor.sh','windsurf.ai','codeium.com') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | group upload_bytes=sum(outB), events=count(), hosts=array_agg_distinct(event.hostname,20) by event.user,event.appname | sort -upload_bytes | nolimit
  merge:
    kind: aggregate
    key_cols:
    - event.user
    - event.appname
    sum_cols:
    - upload_bytes
    - events
    array_cols:
    - hosts
  scope: subject
- id: zia-egress-dlp-high-risk-shadow-it-uploads
  title: High-Risk Shadow IT Uploads
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('icloud','wetransfer','swisstransfer','mega','dropbox','box','telegram','discord','signal','whatsapp','archive.org','mediafire','gofile','file.io') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | group upload_bytes=sum(outB), events=count(), users=estimate_distinct(event.user) by host,event.appname,event.action | sort -upload_bytes | nolimit
  merge:
    kind: aggregate
    key_cols:
    - host
    - event.appname
    - event.action
    sum_cols:
    - upload_bytes
    - events
    approx_cols:
    - users
  scope: subject
- id: zia-egress-dlp-large-uploads-50mb
  title: Large Uploads >50MB
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter outB > 50000000 | columns ts, event.user, event.deviceowner, event.devicehostname, event.hostname, event.url, event.appname, event.requestmethod, event.action, outB, event.urlcategory, event.rulelabel | sort -outB | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-egress-dlp-zia-disable-bypass-events
  title: ZIA Disable / Bypass Events
  notes: 'Source: zia_disable_reason | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia_disable_reason|zia_disable_reason}}' | group events=count(), reasons=array_agg_distinct(DisableReason,20), users=array_agg_distinct(User,20) by UDID | sort -events | nolimit
  merge:
    kind: aggregate
    key_cols:
    - UDID
    sum_cols:
    - events
    array_cols:
    - reasons
    - users
  scope: subject
- id: zia-upload-shadow-it-ai-application-usage-upload-volume
  title: AI Application Usage / Upload Volume
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | filter event.appname contains:anycase('Claude','Anthropic','Codeium','Windsurf','Glean','OpenAI','ChatGPT','Gemini','Copilot','Perplexity','Cursor') OR event.hostname contains:anycase('claude.ai','anthropic.com','openai.com','chatgpt.com','gemini.google.com','perplexity.ai','cursor.com','cursor.sh') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | group upload_bytes=sum(outB), events=count(), destinations=estimate_distinct(event.hostname) by event.user, event.devicehostname, event.appname | sort -upload_bytes | nolimit
  merge:
    kind: aggregate
    key_cols:
    - event.user
    - event.devicehostname
    - event.appname
    sum_cols:
    - upload_bytes
    - events
    approx_cols:
    - destinations
  scope: subject
- id: zia-upload-shadow-it-high-risk-shadow-it-uploads
  title: High-Risk Shadow IT Uploads
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let host=(event.hostname || event.url || event.dns_req || '') | filter host contains:anycase('icloud','wetransfer','swisstransfer','mega','dropbox','box','telegram','discord','signal','whatsapp','archive.org','mediafire','gofile','file.io') | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | group upload_bytes=sum(outB), events=count() by event.user, event.devicehostname, host, event.appname, event.action | sort -upload_bytes | nolimit
  merge:
    kind: aggregate
    key_cols:
    - event.user
    - event.devicehostname
    - host
    - event.appname
    - event.action
    sum_cols:
    - upload_bytes
    - events
  scope: subject
- id: zia-upload-shadow-it-large-uploads-50mb
  title: Large Uploads >50MB
  notes: 'Source: zia | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia|zia}}' | let ts=(event.datetime || timestamp || sca:ingestTime) | let outB=(event.outbytes || ZENTotalBytesTxClient || 0) | filter outB > 50000000 | columns ts.timestamp=ts, event.user, event.devicehostname, event.deviceowner, event.hostname, event.url, event.appname, event.requestmethod, event.action, outB | sort -outB | nolimit
  merge:
    kind: append
  scope: subject
- id: zia-upload-shadow-it-zia-disable-bypass-events
  title: ZIA Disable / Bypass Events
  notes: 'Source: zia_disable_reason | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" serverHost='{{src_zia_disable_reason|zia_disable_reason}}' | group events=count(), reasons=array_agg(DisableReason,20), users=array_agg(User,10) by UDID | sort -events | nolimit
  merge:
    kind: aggregate
    key_cols:
    - UDID
    sum_cols:
    - events
    array_cols:
    - reasons
    - users
  scope: subject
- id: icloud-apple-sync-large-files-written-to-icloud-100mb
  title: Large Files Written to iCloud >100MB
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') OR tgt.file.path contains:anycase('CloudDocs') | filter tgt.file.size > 104857600 | columns ts.timestamp=event.time, endpoint.name, src.process.user, src.process.name, event.type, tgt.file.path, SizeMB=(tgt.file.size/1024/1024), tgt.file.sha1, src.process.storyline.id | sort -SizeMB | nolimit
  merge:
    kind: append
  scope: subject
- id: icloud-apple-sync-large-files-written-to-icloud-100mb-2
  title: Large Files Written to iCloud >100MB
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') OR tgt.file.path contains:anycase('CloudDocs') | filter tgt.file.size > 104857600 | columns event.time, endpoint.name, src.process.user, src.process.name, event.type, tgt.file.path, SizeMB=(tgt.file.size/1024/1024), tgt.file.sha1, src.process.storyline.id | sort -SizeMB | nolimit
  merge:
    kind: append
  scope: subject
- id: icloud-apple-sync-icloud-dns-network
  title: iCloud DNS / Network
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" | union ( event.category='dns' | filter event.dns.request contains:anycase('icloud','icloud-content','apple','mzstatic','cdn-apple','ckdatabase') | columns event.time, endpoint.name, src.process.user, src.process.name, Signal='DNS', Object=event.dns.request, Detail=event.dns.response ), ( event.category='ip' | filter src.process.name in ('bird','cloudd','cloudphotod','nsurlsessiond','sharingd') | columns event.time, endpoint.name, src.process.user, src.process.name, Signal='IP Connect', Object=dst.ip.address, Detail=(dst.as.organization || dst.geo.country) ) | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: icloud-apple-sync-icloud-file-timeline
  title: iCloud File Timeline
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') OR tgt.file.path contains:anycase('CloudDocs') | columns ts.timestamp=event.time, endpoint.name, agent.uuid, src.process.user, src.process.name, event.type, tgt.file.path, tgt.file.size, tgt.file.sha1, src.process.storyline.id | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: icloud-apple-sync-icloud-file-timeline-2
  title: iCloud File Timeline
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='file' | filter tgt.file.path contains:anycase('Mobile Documents') OR tgt.file.path contains:anycase('iCloud Drive') OR tgt.file.path contains:anycase('com~apple~CloudDocs') OR tgt.file.path contains:anycase('CloudDocs') | columns event.time, endpoint.name, agent.uuid, src.process.user, src.process.name, event.type, tgt.file.path, tgt.file.size, tgt.file.sha1, src.process.storyline.id | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: icloud-apple-sync-icloud-processes-sync-daemons
  title: iCloud Processes / Sync Daemons
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='process' | filter src.process.name in ('bird','cloudd','cloudphotod','nsurlsessiond','sharingd') OR src.process.name contains:anycase('icloud','CloudDocs') OR src.process.cmdline contains:anycase('icloud','CloudDocs') | columns ts.timestamp=event.time, endpoint.name, agent.uuid, src.process.user, src.process.name, src.process.image.path, src.process.cmdline, src.process.parent.name, src.process.storyline.id | sort -ts.timestamp | nolimit
  merge:
    kind: append
  scope: subject
- id: icloud-apple-sync-icloud-processes-sync-daemons-2
  title: iCloud Processes / Sync Daemons
  notes: 'Source: edr | Imported/generated for SOC investigation library.'
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, USER_ID_DERIVED, username, detail.payload.Username, detail.payload.UserId, email, login, elogin, account, owner, operator, src.process.user, user.name, event.user, event.deviceowner, log.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, content.email, endpoint.name, agent.uuid, event.hostname, event.devicehostname, 'Source\User') contains "{{entity}}" event.category='process' | filter src.process.name in ('bird','cloudd','cloudphotod','nsurlsessiond','sharingd') OR src.process.name contains:anycase('icloud') OR src.process.cmdline contains:anycase('CloudDocs','icloud') | columns event.time, endpoint.name, agent.uuid, src.process.user, src.process.name, src.process.image.path, src.process.cmdline, src.process.parent.name, src.process.storyline.id | sort -event.time | nolimit
  merge:
    kind: append
  scope: subject
- id: dash-11-raw-quick-pivots-user-source-coverage
  title: User Source Coverage
  notes: Imported from dashboard 'Unified Insider Risk / DLP investigation dashboard combining OpLockdown software inventory, SentinelOne EDR behavior, ZIA egress, iCloud sync, Prompt Security AI activity, Google Workspace, Salesforce, and quick correlation views. Use dashboard-level filters where available; for sources with different identity fields, use the normalized User columns in tables for ad hoc filtering/drilldown.' tab '11. Raw Quick Pivots'.
  pq: any(actor.email, actor.profileId, actor.alternateId, actor.user.email, actor.email_address, user, user.email, userName, USER_ID, email, login, elogin, account, owner, operator, src.process.user, created_by.login, created_by.name, target_user.email, initiated_by.email, initiated_by.username, userIdentity.arn, userIdentity.sessionContext.sessionIssuer, ipAddress, ip, session, event.client_id, event.app_name, event.api_name, id.applicationName, detail.payload.Username, detail.payload.UserId, username, 'Source\ User') contains '*' | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp) by Source=serverHost | sort -EventCount | limit 200
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: coverage
- id: itm-recon-broad-saas-coverage
  title: Broad SaaS user coverage
  notes: 'Generated SOC library detection. Source: all. Designed for investigation execution engine and mapped to insider-risk/MITRE-style behavior.'
  pq: serverHost in ('figma','zoom','netsuite','pagerduty','asana_audit_log_jh','monday_audit_log_jh','clari_audit_log_jh','contentstack','goanywheremft') | filter actor.email contains:anycase('{{entity}}') OR user contains:anycase('{{entity}}') OR email contains:anycase('{{entity}}') | group EventCount=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp), Actions=array_agg_distinct(action || event.name || event_type,20) by Source=serverHost | sort -EventCount | limit 500
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - EventCount
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
- id: mitre-t1078-valid-accounts
  title: Valid account usage across identity sources
  notes: 'Generated SOC library detection. Source: identity. Designed for investigation execution engine and mapped to insider-risk/MITRE-style behavior.'
  pq: serverHost in ('okta','Azure','google_workspace','salesforce-realtime') | filter actor.alternateId contains:anycase('{{entity}}') OR actor.email contains:anycase('{{entity}}') OR UserPrincipalName contains:anycase('{{entity}}') OR detail.payload.Username contains:anycase('{{entity}}') | group Events=count(), FirstSeen.timestamp=min(timestamp), LastSeen.timestamp=max(timestamp), IPs=array_agg_distinct(client.ipAddress || IPAddress || ipAddress || detail.payload.SourceIp,20) by Source=serverHost | sort -Events
  merge:
    kind: aggregate
    key_cols:
    - Source
    sum_cols:
    - Events
    min_cols:
    - FirstSeen.timestamp
    max_cols:
    - LastSeen.timestamp
  scope: subject
